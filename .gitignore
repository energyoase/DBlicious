/target
/target-test
/dist
# Client-WASM-Build-Output — wird lokal/CI via `trunk build` erzeugt, nicht versioniert
/client/dist/
**/target
**/target-test
# isolierte Parallel-Build-Dirs (target-<slug> via --target-dir -j2, siehe parallel-sessions)
target-*/
**/*.rs.bk
.DS_Store
.idea/
.vscode/

# triage state files
.claude/triage-monitor-state.json
.claude/dev-executor-state.json
.claude/scheduled_tasks.lock

# bug-report mirror (the monitor copies ZIPs here for inspection)
bug-reports/

# superpowers brainstorm artifacts (visual companion server state + mockups)
.superpowers/


# CCM runtime
.ccm/
.claude/*-state.json

# transiente Test-Output-Dumps
test_fail_output.txt
