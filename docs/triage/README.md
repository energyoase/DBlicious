# Triage TODOs

Open items live here as `T<NNNN>-<slug>.md`. Closed items are moved
to `done/` by `/dev-executor`. New items are seeded by
`/triage-monitor` or added manually.

See `~/.claude/skills/triage-monitor/SKILL.md` for the file format.
