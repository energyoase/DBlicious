# Q0005 — Named Views (In-Place Column-Editor + 3-Layer-Overlay) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Built per [`docs/superpowers/specs/2026-05-21-q0005-named-views-design.md`](../specs/2026-05-21-q0005-named-views-design.md): mehrere benannte Views pro Entity-Typ mit 3-Layer-Overlay (Global → Group → User), durch URL-Param adressierbar, mit In-Place-Editor (Header-Popover) auf der laufenden Tabelle.

**Architecture:** Neue `entity_views`-Tabelle + Server-Resolver, der pro Request einen 3-Layer-Merge erzeugt; bestehende `entitySettings`-GraphQL-Query bleibt signaturgleich (intern resolved → backwards-compat). Client erweitert um View-Picker via Query-Param, Edit-Mode-Toggle in TopMenu, neue `ColumnEditorPopover`-Komponente, optimistic locking auf Save.

**Tech Stack:** Rust workspace (`shared` / `server` / `client`), SeaORM + SQLite (in-memory in dev), async-graphql, Leptos 0.7 CSR + WASM, Trunk, Fluent (i18n).

---

## File Structure

**Created:**
- `shared/src/view.rs` — Wire-Types: `EntityView`, `ViewLayer`, `ViewPropertyOverride`
- `shared/tests/view_wire_format.rs` — JSON-Format-Pinning-Tests
- `server/src/entity/entity_views.rs` — SeaORM-Entity
- `server/src/views.rs` — Resolver + Merge-Logik + Unit-Tests
- `server/tests/entity_views.rs` — Persistenz- und Bootstrap-Tests
- `client/src/components/table/column_editor/mod.rs` — Pure-Function-Logik (`apply_pending_overrides`, `compute_reorder`)
- `client/src/components/table/column_editor/popover.rs` — `ColumnEditorPopover`-Komponente

**Modified:**
- `shared/src/lib.rs` — Re-exports der neuen Typen
- `server/src/entity/mod.rs` — Entity-Modul registrieren
- `server/src/db.rs` — `create_table_from_entity` + `seed_entity_views_from_example` einhängen
- `server/src/data.rs` — `seed_entity_views_from_example` implementieren + CRUD-Helpers
- `server/src/schema.rs` — neue GraphQL-Types/Queries/Mutations, `entitySettings` rewiren
- `server/src/lib.rs` — `pub mod views;`
- `server/tests/e2e.rs` — GraphQL-Erweiterungstests
- `client/src/graphql/queries.rs` — 4 neue Adapter-Funktionen
- `client/src/components/table/mod.rs` — `pub mod column_editor;`
- `client/src/components/table/filters/registry.rs` — `compatible_for` + `FilterDescriptor`
- `client/src/components/table/formatters.rs` — `FormatterRegistry` + `compatible_for`
- `client/src/components/table/view.rs` — Header-Klick + Drag-Reorder-Handler im Edit-Mode
- `client/src/routes/mod.rs` — `EntityListPage`: View-State, Pending-Buffer, Save-Flow, TopMenu-Erweiterung
- `client/locales/{de,en,fr}/main.ftl` — neue i18n-Keys (`column-editor-*`, `table-actions-*`, `formatter.*`)

---

## Phase A — Shared Wire-Types

### Task A1: Definiere `ViewLayer`, `EntityView`, `ViewPropertyOverride` als Wire-Typen

**Files:**
- Create: `shared/src/view.rs`
- Modify: `shared/src/lib.rs:19` (Modul registrieren) + `:53` (Re-Exports)

- [ ] **Step 1: Schreibe den Failing Test** in `shared/tests/view_wire_format.rs`

```rust
//! Pin-Test fuer das Wire-Format der Named-Views (Q0005).
//! Bricht in CI, wenn jemand camelCase/Tag/skip_serializing_if veraendert.

use shared::view::{EntityView, ViewLayer, ViewPropertyOverride};
use shared::Visibility;

#[test]
fn entity_view_global_serializes_to_pinned_camelcase_json() {
    let v = EntityView {
        id: "v-1".into(),
        entity_type: "order".into(),
        view_name: "default".into(),
        layer: ViewLayer::Global,
        owner_id: None,
        properties: vec![ViewPropertyOverride {
            key: "amount".into(),
            visibility: Some(Visibility::Visible),
            order: Some(2),
            min_width: None,
            label_override_key: None,
            sortable: None,
            filter_id_override: None,
            formatter_id_override: None,
        }],
        default_filter: None,
        default_sort: None,
        default_page_size: None,
        version: 1,
        updated_at: "2026-05-21T00:00:00Z".into(),
        updated_by: Some("u-1".into()),
    };
    let s = serde_json::to_string(&v).unwrap();
    let expected = r#"{"id":"v-1","entityType":"order","viewName":"default","layer":"global","ownerId":null,"properties":[{"key":"amount","visibility":"visible","order":2}],"defaultFilter":null,"defaultSort":null,"defaultPageSize":null,"version":1,"updatedAt":"2026-05-21T00:00:00Z","updatedBy":"u-1"}"#;
    assert_eq!(s, expected);
}

#[test]
fn view_layer_serializes_lowercase() {
    assert_eq!(serde_json::to_string(&ViewLayer::Global).unwrap(), "\"global\"");
    assert_eq!(serde_json::to_string(&ViewLayer::Group).unwrap(),  "\"group\"");
    assert_eq!(serde_json::to_string(&ViewLayer::User).unwrap(),   "\"user\"");
}

#[test]
fn empty_overrides_drop_via_skip_serializing_if() {
    let o = ViewPropertyOverride { key: "x".into(), ..Default::default() };
    let s = serde_json::to_string(&o).unwrap();
    assert_eq!(s, r#"{"key":"x"}"#);
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cargo test -p shared --test view_wire_format`
Expected: FAIL with "unresolved import `shared::view`" (Modul existiert noch nicht).

- [ ] **Step 3: Implementiere `shared/src/view.rs`**

```rust
//! Phase 1.9 / Q0005 — Named Views mit 3-Layer-Overlay.
//!
//! `EntityView` ist der Wire-Typ fuer eine *gespeicherte* View-Schicht
//! (Global/Group/User). Der Resolver auf Server-Seite stapelt diese
//! Schichten zu `EntitySettings`-aequivalenten Effective-Werten.

use serde::{Deserialize, Serialize};

use crate::settings::Visibility;
use crate::{FilterCriteria, Sort};

#[derive(Debug, Clone, Copy, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "camelCase")]
pub enum ViewLayer {
    Global,
    Group,
    User,
}

/// Sparse Property-Override fuer eine einzelne Spalte.
///
/// Merge-Semantik im Resolver: ein `Some(_)` schlaegt den darunter
/// liegenden Layer; `None` = inherit.
#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Default)]
#[serde(rename_all = "camelCase")]
pub struct ViewPropertyOverride {
    pub key: String,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub visibility: Option<Visibility>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub order: Option<i32>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub min_width: Option<u32>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub label_override_key: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub sortable: Option<bool>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub filter_id_override: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub formatter_id_override: Option<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
#[serde(rename_all = "camelCase")]
pub struct EntityView {
    pub id: String,
    pub entity_type: String,
    pub view_name: String,
    pub layer: ViewLayer,
    pub owner_id: Option<String>,
    pub properties: Vec<ViewPropertyOverride>,
    pub default_filter: Option<FilterCriteria>,
    pub default_sort: Option<Sort>,
    pub default_page_size: Option<u32>,
    pub version: i32,
    pub updated_at: String,
    pub updated_by: Option<String>,
}

/// Verwendet vom Server-Resolver. Im Wire-Format als Debug/Audit-Info.
#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "camelCase")]
pub struct ResolvedLayerRef {
    pub layer: ViewLayer,
    pub view_id: String,
    pub owner_id: Option<String>,
    pub version: i32,
}
```

- [ ] **Step 4: Registriere Modul + Re-Exports** in `shared/src/lib.rs`

Nach `pub mod validation;` einfuegen:
```rust
pub mod view;
```

Im Re-Export-Block (nach `pub use validation::...`):
```rust
pub use view::{EntityView, ResolvedLayerRef, ViewLayer, ViewPropertyOverride};
```

- [ ] **Step 5: Run test to verify it passes**

Run: `cargo test -p shared --test view_wire_format`
Expected: PASS — 3 Tests gruen.

Run zur Sicherheit auch: `cargo test -p shared` (alle bestehenden Tests muessen weiterhin passen).
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add shared/src/view.rs shared/src/lib.rs shared/tests/view_wire_format.rs
git commit -m "feat(shared): EntityView/ViewLayer/ViewPropertyOverride wire types (Q0005 A1)"
```

---

## Phase B — Server: SeaORM Entity + DB-Schema

### Task B1: SeaORM-Entity `entity_views`

**Files:**
- Create: `server/src/entity/entity_views.rs`
- Modify: `server/src/entity/mod.rs` (Modul exportieren)

- [ ] **Step 1: Erstelle `server/src/entity/entity_views.rs`**

```rust
//! Persistenz der Named-Views (Q0005).
//!
//! Eine Row pro `(entity_type, view_name, layer, owner_id)`. Schichten
//! werden vom Resolver in `server/src/views.rs` zu einem `EntitySettings`-
//! Aequivalent gemerged.

use sea_orm::entity::prelude::*;
use serde::{Deserialize, Serialize};

#[derive(Clone, Debug, PartialEq, DeriveEntityModel, Serialize, Deserialize)]
#[sea_orm(table_name = "entity_views")]
pub struct Model {
    #[sea_orm(primary_key, auto_increment = false, column_type = "Text")]
    pub id: String,
    #[sea_orm(column_type = "Text")]
    pub entity_type: String,
    #[sea_orm(column_type = "Text")]
    pub view_name: String,
    /// "global" | "group" | "user"  — als String gespeichert, weil
    /// die Wire-Form lower-case ist und kein `EnumIter`-Roundtrip
    /// gebraucht wird.
    #[sea_orm(column_type = "Text")]
    pub layer: String,
    /// NULL gdw `layer = "global"`. SeaORM macht aus dem UNIQUE-Index
    /// einen partiellen Index — Code-seitig zusaetzlich enforced in
    /// `views::insert_view`.
    #[sea_orm(column_type = "Text", nullable)]
    pub owner_id: Option<String>,
    /// JSON-Blob: `{ properties, defaultFilter, defaultSort, defaultPageSize }`.
    #[sea_orm(column_type = "Text")]
    pub payload: String,
    pub version: i32,
    #[sea_orm(column_type = "Text", nullable)]
    pub updated_by: Option<String>,
    #[sea_orm(column_type = "Text")]
    pub updated_at: String,
}

#[derive(Copy, Clone, Debug, EnumIter, DeriveRelation)]
pub enum Relation {}

impl ActiveModelBehavior for ActiveModel {}
```

- [ ] **Step 2: Registriere in `server/src/entity/mod.rs`**

Pruefe das bestehende Muster (`grep -n "^pub mod" server/src/entity/mod.rs`). Fuege analog hinzu:

```rust
pub mod entity_views;
```

(Position alphabetisch zwischen `entities` und `permissions` o.aehnl.).

- [ ] **Step 3: Verify it compiles**

Run: `cargo check -p server`
Expected: keine Compile-Fehler (Warnings durch unused okay).

- [ ] **Step 4: Commit**

```bash
git add server/src/entity/entity_views.rs server/src/entity/mod.rs
git commit -m "feat(server): SeaORM entity for entity_views table (Q0005 B1)"
```

### Task B2: Tabellenerzeugung in `db::init`

**Files:**
- Modify: `server/src/db.rs` (Tabelle hinzufuegen + UNIQUE-Index)

- [ ] **Step 1: Patch `init_schema` in `server/src/db.rs`**

Suche den `let mut tables = vec![...]`-Block (ca. Zeile 70-105). Fuege als zusaetzlichen Eintrag hinzu (Reihenfolge: nach `plugin_invocations`):

```rust
// Q0005: Named Views — 1 Row pro (entity_type, view_name, layer, owner_id).
schema.create_table_from_entity(entity::entity_views::Entity),
```

Nach der `for t in &mut tables`-Schleife (gleicher Block) UNIQUE-Index ergaenzen:

```rust
// UNIQUE (entity_type, view_name, layer, owner_id)
// SeaORM hat hier keine bequeme Builder-API mit NULL-Semantik; raw SQL
// bleibt sauber.
db.execute_unprepared(
    "CREATE UNIQUE INDEX IF NOT EXISTS uq_entity_views \
     ON entity_views(entity_type, view_name, layer, IFNULL(owner_id, ''))"
)
.await?;
```

- [ ] **Step 2: Schreibe Smoke-Test** in `server/tests/entity_views.rs` (NEUE Datei)

```rust
//! Persistenz-Tests fuer entity_views (Q0005).

use sea_orm::{ActiveModelTrait, ActiveValue, EntityTrait};
use server::{entity, fresh_test_setup};

#[tokio::test]
#[serial_test::serial]
async fn entity_views_table_exists_after_init() {
    let _ = fresh_test_setup().await;
    let db = server::db::conn();
    let count = entity::entity_views::Entity::find()
        .count(&db)
        .await
        .expect("query");
    assert_eq!(count, 0, "Tabelle existiert und ist leer nach init");
}

#[tokio::test]
#[serial_test::serial]
async fn insert_and_read_back_a_view_row() {
    let _ = fresh_test_setup().await;
    let db = server::db::conn();
    entity::entity_views::ActiveModel {
        id:          ActiveValue::Set("v-1".into()),
        entity_type: ActiveValue::Set("order".into()),
        view_name:   ActiveValue::Set("default".into()),
        layer:       ActiveValue::Set("global".into()),
        owner_id:    ActiveValue::Set(None),
        payload:     ActiveValue::Set("{\"properties\":[]}".into()),
        version:     ActiveValue::Set(1),
        updated_by:  ActiveValue::Set(Some("system".into())),
        updated_at:  ActiveValue::Set("2026-05-21T00:00:00Z".into()),
    }
    .insert(&db)
    .await
    .expect("insert");

    let row = entity::entity_views::Entity::find_by_id("v-1".to_string())
        .one(&db)
        .await
        .expect("query")
        .expect("found");
    assert_eq!(row.entity_type, "order");
    assert_eq!(row.layer, "global");
    assert_eq!(row.version, 1);
}
```

- [ ] **Step 3: Run test (expect compile error wegen `conn` Sichtbarkeit)**

Run: `cargo test -p server --test entity_views`
Expected: vermutlich Compile-Fehler. Wenn `db::conn` nicht `pub`, in `server/src/db.rs` exportieren (oder via `server::data::*`-Pfad benutzen, falls dort vorhanden).

Falls Anpassung noetig: in `server/src/db.rs` die existierende `conn()`-Funktion `pub` machen (sie ist wahrscheinlich crate-private). Begruendung im Code-Kommentar: "Test-Zugriff fuer entity_views.rs".

- [ ] **Step 4: Run test again, expect pass**

Run: `cargo test -p server --test entity_views --target-dir target-test`
Expected: PASS — beide Tests gruen. (`--target-dir target-test` weil ggf. `server.exe` durch laufenden Dev-Server file-locked ist; `target-test/` ist in `.gitignore`.)

- [ ] **Step 5: Commit**

```bash
git add server/src/db.rs server/tests/entity_views.rs
# Falls conn() angepasst:
# git add server/src/db.rs
git commit -m "feat(server): create entity_views table + UNIQUE index (Q0005 B2)"
```

---

## Phase C — Server: Data-Layer-Helpers

### Task C1: CRUD-Helper fuer `entity_views`

**Files:**
- Modify: `server/src/data.rs` (neue Funktionen am Ende einfuegen, nach den anderen `seed_*`-Funktionen)

- [ ] **Step 1: Schreibe Tests** in `server/tests/entity_views.rs` (anhaengen)

```rust
use shared::view::{EntityView, ViewLayer, ViewPropertyOverride};
use shared::Visibility;
use server::data;

fn sample_view(name: &str, layer: ViewLayer, owner_id: Option<&str>) -> EntityView {
    EntityView {
        id: format!("v-{name}-{layer:?}"),
        entity_type: "order".into(),
        view_name: name.into(),
        layer,
        owner_id: owner_id.map(String::from),
        properties: vec![ViewPropertyOverride {
            key: "amount".into(),
            visibility: Some(Visibility::Visible),
            ..Default::default()
        }],
        default_filter: None,
        default_sort: None,
        default_page_size: None,
        version: 1,
        updated_at: "2026-05-21T00:00:00Z".into(),
        updated_by: Some("system".into()),
    }
}

#[tokio::test]
#[serial_test::serial]
async fn upsert_view_inserts_then_updates() {
    let _ = server::fresh_test_setup().await;
    let v = sample_view("default", ViewLayer::Global, None);
    data::upsert_entity_view(&v).await.expect("insert");
    let read = data::find_entity_view("order", "default", ViewLayer::Global, None)
        .await
        .expect("read");
    assert_eq!(read.unwrap().version, 1);

    let mut v2 = v.clone();
    v2.version = 2;
    data::upsert_entity_view(&v2).await.expect("update");
    let read2 = data::find_entity_view("order", "default", ViewLayer::Global, None)
        .await
        .expect("read");
    assert_eq!(read2.unwrap().version, 2);
}

#[tokio::test]
#[serial_test::serial]
async fn find_views_for_entity_lists_per_layer() {
    let _ = server::fresh_test_setup().await;
    data::upsert_entity_view(&sample_view("default", ViewLayer::Global, None)).await.unwrap();
    data::upsert_entity_view(&sample_view("default", ViewLayer::Group,  Some("g-1"))).await.unwrap();
    data::upsert_entity_view(&sample_view("default", ViewLayer::User,   Some("u-1"))).await.unwrap();
    let summary = data::find_entity_views("order").await.unwrap();
    assert_eq!(summary.len(), 1, "Eine View namens 'default'");
    assert!(summary[0].layers.contains(&ViewLayer::Global));
    assert!(summary[0].layers.contains(&ViewLayer::Group));
    assert!(summary[0].layers.contains(&ViewLayer::User));
}

#[tokio::test]
#[serial_test::serial]
async fn delete_view_removes_only_target_layer() {
    let _ = server::fresh_test_setup().await;
    data::upsert_entity_view(&sample_view("default", ViewLayer::Global, None)).await.unwrap();
    data::upsert_entity_view(&sample_view("default", ViewLayer::User,   Some("u-1"))).await.unwrap();
    data::delete_entity_view("order", "default", ViewLayer::User, Some("u-1")).await.unwrap();
    assert!(data::find_entity_view("order", "default", ViewLayer::Global, None).await.unwrap().is_some());
    assert!(data::find_entity_view("order", "default", ViewLayer::User,   Some("u-1")).await.unwrap().is_none());
}
```

- [ ] **Step 2: Run tests to verify they fail (compile error fehlende API)**

Run: `cargo test -p server --test entity_views --target-dir target-test`
Expected: FAIL — `cannot find function 'upsert_entity_view' in module 'data'`.

- [ ] **Step 3: Implementiere die Helpers** in `server/src/data.rs` (am Ende der Datei vor `mod tests`):

```rust
// =============================================================================
// Q0005: Named Views — CRUD-Helpers
// =============================================================================

use shared::view::{EntityView, ViewLayer, ViewPropertyOverride};

/// JSON-Form, die im `payload`-Feld der `entity_views`-Tabelle liegt.
/// Nur das, was *nicht* in den eigenen Spalten redundant gehalten wird.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
#[serde(rename_all = "camelCase")]
struct ViewPayload {
    properties:        Vec<ViewPropertyOverride>,
    #[serde(default)]
    default_filter:    Option<shared::FilterCriteria>,
    #[serde(default)]
    default_sort:      Option<shared::Sort>,
    #[serde(default)]
    default_page_size: Option<u32>,
}

pub struct EntityViewSummary {
    pub view_name:  String,
    pub layers:     Vec<ViewLayer>,
    pub updated_at: String,
}

fn layer_str(l: ViewLayer) -> &'static str {
    match l {
        ViewLayer::Global => "global",
        ViewLayer::Group  => "group",
        ViewLayer::User   => "user",
    }
}

fn layer_from_str(s: &str) -> Option<ViewLayer> {
    match s {
        "global" => Some(ViewLayer::Global),
        "group"  => Some(ViewLayer::Group),
        "user"   => Some(ViewLayer::User),
        _        => None,
    }
}

fn assert_layer_invariant(layer: ViewLayer, owner_id: Option<&str>) -> Result<(), String> {
    let global_no_owner = layer == ViewLayer::Global && owner_id.is_none();
    let scoped_has_owner = matches!(layer, ViewLayer::Group | ViewLayer::User) && owner_id.is_some();
    if global_no_owner || scoped_has_owner {
        Ok(())
    } else {
        Err(format!("Invalides Layer/Owner-Paar: layer={layer:?}, owner_id={owner_id:?}"))
    }
}

pub async fn upsert_entity_view(v: &EntityView) -> Result<(), String> {
    assert_layer_invariant(v.layer, v.owner_id.as_deref())?;
    let payload = ViewPayload {
        properties:        v.properties.clone(),
        default_filter:    v.default_filter.clone(),
        default_sort:      v.default_sort.clone(),
        default_page_size: v.default_page_size,
    };
    let payload_json = serde_json::to_string(&payload).map_err(|e| e.to_string())?;
    let db = &conn();
    use sea_orm::{ActiveValue, EntityTrait, QueryFilter, ColumnTrait};
    let existing = entity::entity_views::Entity::find()
        .filter(entity::entity_views::Column::EntityType.eq(v.entity_type.clone()))
        .filter(entity::entity_views::Column::ViewName.eq(v.view_name.clone()))
        .filter(entity::entity_views::Column::Layer.eq(layer_str(v.layer)))
        .filter(match &v.owner_id {
            Some(o) => entity::entity_views::Column::OwnerId.eq(o.clone()),
            None    => entity::entity_views::Column::OwnerId.is_null(),
        })
        .one(db)
        .await
        .map_err(|e| e.to_string())?;
    match existing {
        Some(row) => {
            let mut am: entity::entity_views::ActiveModel = row.into();
            am.payload    = ActiveValue::Set(payload_json);
            am.version    = ActiveValue::Set(v.version);
            am.updated_by = ActiveValue::Set(v.updated_by.clone());
            am.updated_at = ActiveValue::Set(v.updated_at.clone());
            am.update(db).await.map_err(|e| e.to_string())?;
        }
        None => {
            entity::entity_views::ActiveModel {
                id:          ActiveValue::Set(v.id.clone()),
                entity_type: ActiveValue::Set(v.entity_type.clone()),
                view_name:   ActiveValue::Set(v.view_name.clone()),
                layer:       ActiveValue::Set(layer_str(v.layer).into()),
                owner_id:    ActiveValue::Set(v.owner_id.clone()),
                payload:     ActiveValue::Set(payload_json),
                version:     ActiveValue::Set(v.version),
                updated_by:  ActiveValue::Set(v.updated_by.clone()),
                updated_at:  ActiveValue::Set(v.updated_at.clone()),
            }
            .insert(db)
            .await
            .map_err(|e| e.to_string())?;
        }
    }
    Ok(())
}

pub async fn find_entity_view(
    entity_type: &str,
    view_name:   &str,
    layer:       ViewLayer,
    owner_id:    Option<&str>,
) -> Result<Option<EntityView>, String> {
    use sea_orm::{ColumnTrait, EntityTrait, QueryFilter};
    let db = &conn();
    let row = entity::entity_views::Entity::find()
        .filter(entity::entity_views::Column::EntityType.eq(entity_type))
        .filter(entity::entity_views::Column::ViewName.eq(view_name))
        .filter(entity::entity_views::Column::Layer.eq(layer_str(layer)))
        .filter(match owner_id {
            Some(o) => entity::entity_views::Column::OwnerId.eq(o),
            None    => entity::entity_views::Column::OwnerId.is_null(),
        })
        .one(db)
        .await
        .map_err(|e| e.to_string())?;
    Ok(row.map(row_to_view))
}

pub async fn find_entity_views(entity_type: &str) -> Result<Vec<EntityViewSummary>, String> {
    use sea_orm::{ColumnTrait, EntityTrait, QueryFilter};
    use std::collections::BTreeMap;
    let db = &conn();
    let rows = entity::entity_views::Entity::find()
        .filter(entity::entity_views::Column::EntityType.eq(entity_type))
        .all(db)
        .await
        .map_err(|e| e.to_string())?;
    let mut by_name: BTreeMap<String, EntityViewSummary> = BTreeMap::new();
    for r in rows {
        let layer = layer_from_str(&r.layer).ok_or_else(|| format!("Unbekannter Layer: '{}'", r.layer))?;
        let entry = by_name.entry(r.view_name.clone()).or_insert_with(|| EntityViewSummary {
            view_name:  r.view_name.clone(),
            layers:     Vec::new(),
            updated_at: r.updated_at.clone(),
        });
        if !entry.layers.contains(&layer) {
            entry.layers.push(layer);
        }
        if r.updated_at > entry.updated_at {
            entry.updated_at = r.updated_at;
        }
    }
    Ok(by_name.into_values().collect())
}

pub async fn delete_entity_view(
    entity_type: &str,
    view_name:   &str,
    layer:       ViewLayer,
    owner_id:    Option<&str>,
) -> Result<(), String> {
    use sea_orm::{ColumnTrait, EntityTrait, QueryFilter};
    let db = &conn();
    entity::entity_views::Entity::delete_many()
        .filter(entity::entity_views::Column::EntityType.eq(entity_type))
        .filter(entity::entity_views::Column::ViewName.eq(view_name))
        .filter(entity::entity_views::Column::Layer.eq(layer_str(layer)))
        .filter(match owner_id {
            Some(o) => entity::entity_views::Column::OwnerId.eq(o),
            None    => entity::entity_views::Column::OwnerId.is_null(),
        })
        .exec(db)
        .await
        .map_err(|e| e.to_string())?;
    Ok(())
}

fn row_to_view(r: entity::entity_views::Model) -> EntityView {
    let payload: ViewPayload = serde_json::from_str(&r.payload).unwrap_or(ViewPayload {
        properties: Vec::new(),
        default_filter: None, default_sort: None, default_page_size: None,
    });
    EntityView {
        id:           r.id,
        entity_type:  r.entity_type,
        view_name:    r.view_name,
        layer:        layer_from_str(&r.layer).unwrap_or(ViewLayer::Global),
        owner_id:     r.owner_id,
        properties:   payload.properties,
        default_filter:    payload.default_filter,
        default_sort:      payload.default_sort,
        default_page_size: payload.default_page_size,
        version:      r.version,
        updated_by:   r.updated_by,
        updated_at:   r.updated_at,
    }
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cargo test -p server --test entity_views --target-dir target-test`
Expected: PASS — alle Tests aus B2 + C1 gruen (5 Tests).

- [ ] **Step 5: Commit**

```bash
git add server/src/data.rs server/tests/entity_views.rs
git commit -m "feat(server): CRUD helpers for entity_views (Q0005 C1)"
```

---

## Phase D — Server: Resolver

### Task D1: Merge-Logik als pure Funktion

**Files:**
- Create: `server/src/views.rs`
- Modify: `server/src/lib.rs` (`pub mod views;` registrieren)

- [ ] **Step 1: Schreibe Tests** in `server/src/views.rs` (Test-Modul am Dateiende)

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use shared::view::{EntityView, ViewLayer, ViewPropertyOverride};
    use shared::Visibility;

    fn ov(key: &str) -> ViewPropertyOverride {
        ViewPropertyOverride { key: key.into(), ..Default::default() }
    }

    fn view(layer: ViewLayer, owner_id: Option<&str>, overrides: Vec<ViewPropertyOverride>) -> EntityView {
        EntityView {
            id: format!("v-{layer:?}-{owner_id:?}"),
            entity_type: "order".into(),
            view_name: "default".into(),
            layer,
            owner_id: owner_id.map(String::from),
            properties: overrides,
            default_filter: None, default_sort: None, default_page_size: None,
            version: 1,
            updated_at: "2026-05-21T00:00:00Z".into(),
            updated_by: None,
        }
    }

    #[test]
    fn only_global_returns_global_properties() {
        let global = view(ViewLayer::Global, None, vec![
            ViewPropertyOverride { key: "amount".into(), visibility: Some(Visibility::Visible), order: Some(1), ..Default::default() },
        ]);
        let r = merge_layers(Some(global), Vec::new(), None);
        assert_eq!(r.properties.len(), 1);
        assert_eq!(r.properties[0].order, 1);
    }

    #[test]
    fn user_overrides_group_overrides_global_field_by_field() {
        let global = view(ViewLayer::Global, None, vec![
            ViewPropertyOverride { key: "amount".into(), visibility: Some(Visibility::Visible), order: Some(1), min_width: Some(80), ..Default::default() },
        ]);
        let group = view(ViewLayer::Group, Some("g-1"), vec![
            ViewPropertyOverride { key: "amount".into(), order: Some(2), ..Default::default() },
        ]);
        let user = view(ViewLayer::User, Some("u-1"), vec![
            ViewPropertyOverride { key: "amount".into(), min_width: Some(120), ..Default::default() },
        ]);
        let r = merge_layers(Some(global), vec![group], Some(user));
        let p = &r.properties[0];
        assert_eq!(p.visibility, Visibility::Visible, "von Global");
        assert_eq!(p.order, 2, "von Group");
        assert_eq!(p.min_width, Some(120), "von User");
    }

    #[test]
    fn two_groups_are_merged_in_id_sort_order() {
        // Group "g-a" setzt order=10; Group "g-b" setzt order=20.
        // Da User > Group, und es nur Group + Global gibt: spaeter im Stack gewinnt.
        // Reihenfolge: g-a first, g-b last → b ueberschreibt a.
        let global = view(ViewLayer::Global, None, vec![
            ViewPropertyOverride { key: "amount".into(), order: Some(0), ..Default::default() },
        ]);
        let group_a = view(ViewLayer::Group, Some("g-a"), vec![
            ViewPropertyOverride { key: "amount".into(), order: Some(10), ..Default::default() },
        ]);
        let group_b = view(ViewLayer::Group, Some("g-b"), vec![
            ViewPropertyOverride { key: "amount".into(), order: Some(20), ..Default::default() },
        ]);
        // Wichtig: vor merge_layers werden die Gruppen schon sortiert geliefert.
        let r = merge_layers(Some(global), vec![group_a, group_b], None);
        assert_eq!(r.properties[0].order, 20, "g-b > g-a > global");
    }

    #[test]
    fn sparse_overrides_inherit_unset_fields() {
        let global = view(ViewLayer::Global, None, vec![
            ViewPropertyOverride {
                key: "amount".into(),
                visibility: Some(Visibility::Visible),
                order: Some(1),
                min_width: Some(80),
                label_override_key: Some("col.amount".into()),
                sortable: Some(true),
                filter_id_override: Some("range".into()),
                formatter_id_override: Some("money-symbol".into()),
            },
        ]);
        let user = view(ViewLayer::User, Some("u-1"), vec![
            ov("amount") // alles None
        ]);
        let r = merge_layers(Some(global), Vec::new(), Some(user));
        let p = &r.properties[0];
        assert_eq!(p.order, 1);
        assert_eq!(p.min_width, Some(80));
        assert_eq!(p.label_override_key, Some("col.amount".into()));
        assert!(p.sortable);
    }

    #[test]
    fn no_layers_yields_empty_resolved() {
        let r = merge_layers(None, Vec::new(), None);
        assert_eq!(r.properties.len(), 0);
        assert!(r.default_filter.is_none());
        assert!(r.default_sort.is_none());
        assert!(r.default_page_size.is_none());
    }
}
```

- [ ] **Step 2: Run tests** — Compile-Fehler erwartet (Modul existiert noch nicht).

Run: `cargo test -p server views::tests --target-dir target-test`
Expected: FAIL — `unresolved module`.

- [ ] **Step 3: Implementiere `server/src/views.rs`**

```rust
//! Q0005 Resolver — staplet Global → Group(*) → User-Layer.
//!
//! Per Property-Key wird Feld-fuer-Feld gemerged: `Some(_)` schlaegt
//! `Some(_)` der darunter liegenden Schicht; `None` = inherit. Group-Stack
//! wird vom Aufrufer in `group.id`-sortierter Reihenfolge geliefert.

use shared::settings::PropertySettings;
use shared::view::{EntityView, ResolvedLayerRef, ViewLayer, ViewPropertyOverride};
use shared::{FilterCriteria, SecurityUser, Sort, Visibility};

pub struct ResolvedView {
    pub entity_type:       String,
    pub view_name:         String,
    pub properties:        Vec<PropertySettings>,
    pub default_filter:    Option<FilterCriteria>,
    pub default_sort:      Option<Sort>,
    pub default_page_size: Option<u32>,
    pub provenance:        Vec<ResolvedLayerRef>,
}

/// Pure Funktion. Group-Stack erwartet stable-sorted nach `group.id`.
pub fn merge_layers(
    base:   Option<EntityView>,
    groups: Vec<EntityView>,
    user:   Option<EntityView>,
) -> ResolvedView {
    let entity_type = base.as_ref()
        .or(groups.first())
        .or(user.as_ref())
        .map(|v| v.entity_type.clone())
        .unwrap_or_default();
    let view_name = base.as_ref()
        .or(groups.first())
        .or(user.as_ref())
        .map(|v| v.view_name.clone())
        .unwrap_or_default();

    let mut accum: std::collections::BTreeMap<String, ViewPropertyOverride> = std::collections::BTreeMap::new();
    let mut default_filter:    Option<FilterCriteria> = None;
    let mut default_sort:      Option<Sort>           = None;
    let mut default_page_size: Option<u32>            = None;
    let mut provenance:        Vec<ResolvedLayerRef>  = Vec::new();

    let push_layer = |accum: &mut std::collections::BTreeMap<String, ViewPropertyOverride>,
                      v: &EntityView| {
        for ov in &v.properties {
            let entry = accum.entry(ov.key.clone()).or_insert_with(|| ViewPropertyOverride {
                key: ov.key.clone(), ..Default::default()
            });
            if ov.visibility.is_some()            { entry.visibility            = ov.visibility; }
            if ov.order.is_some()                 { entry.order                 = ov.order; }
            if ov.min_width.is_some()             { entry.min_width             = ov.min_width; }
            if ov.label_override_key.is_some()    { entry.label_override_key    = ov.label_override_key.clone(); }
            if ov.sortable.is_some()              { entry.sortable              = ov.sortable; }
            if ov.filter_id_override.is_some()    { entry.filter_id_override    = ov.filter_id_override.clone(); }
            if ov.formatter_id_override.is_some() { entry.formatter_id_override = ov.formatter_id_override.clone(); }
        }
    };

    if let Some(b) = &base {
        push_layer(&mut accum, b);
        default_filter    = b.default_filter.clone();
        default_sort      = b.default_sort.clone();
        default_page_size = b.default_page_size;
        provenance.push(ResolvedLayerRef { layer: ViewLayer::Global, view_id: b.id.clone(), owner_id: b.owner_id.clone(), version: b.version });
    }
    for g in &groups {
        push_layer(&mut accum, g);
        if g.default_filter.is_some()    { default_filter    = g.default_filter.clone(); }
        if g.default_sort.is_some()      { default_sort      = g.default_sort.clone(); }
        if g.default_page_size.is_some() { default_page_size = g.default_page_size; }
        provenance.push(ResolvedLayerRef { layer: ViewLayer::Group, view_id: g.id.clone(), owner_id: g.owner_id.clone(), version: g.version });
    }
    if let Some(u) = &user {
        push_layer(&mut accum, u);
        if u.default_filter.is_some()    { default_filter    = u.default_filter.clone(); }
        if u.default_sort.is_some()      { default_sort      = u.default_sort.clone(); }
        if u.default_page_size.is_some() { default_page_size = u.default_page_size; }
        provenance.push(ResolvedLayerRef { layer: ViewLayer::User, view_id: u.id.clone(), owner_id: u.owner_id.clone(), version: u.version });
    }

    // Konvertiere die akkumulierten Overrides in vollstaendige `PropertySettings`.
    // Feldwerte ohne Some(_) bekommen Default-Werte aus PropertySettings.
    let properties: Vec<PropertySettings> = accum.into_values().map(|ov| PropertySettings {
        key:                ov.key,
        visibility:         ov.visibility.unwrap_or(Visibility::Visible),
        access:             shared::settings::PropertyAccess::default(),
        load_method:        shared::settings::LoadMethod::default(),
        order:              ov.order.unwrap_or(i32::MAX),
        label_override_key: ov.label_override_key,
        min_width:          ov.min_width,
    }).collect();

    ResolvedView {
        entity_type, view_name, properties,
        default_filter, default_sort, default_page_size,
        provenance,
    }
}

/// Laedt die drei Layer aus der DB und ruft `merge_layers` auf.
pub async fn resolve_view(
    entity_type: &str,
    view_name:   &str,
    user:        Option<&SecurityUser>,
) -> ResolvedView {
    use crate::data;
    let global = data::find_entity_view(entity_type, view_name, ViewLayer::Global, None).await.unwrap_or(None);

    let mut groups: Vec<EntityView> = Vec::new();
    if let Some(u) = user {
        let mut gids: Vec<&str> = u.group_ids.iter().map(String::as_str).collect();
        gids.sort(); // deterministisch
        for g in gids {
            if let Ok(Some(v)) = data::find_entity_view(entity_type, view_name, ViewLayer::Group, Some(g)).await {
                groups.push(v);
            }
        }
    }
    let user_v = match user {
        Some(u) => data::find_entity_view(entity_type, view_name, ViewLayer::User, Some(&u.id)).await.unwrap_or(None),
        None    => None,
    };
    merge_layers(global, groups, user_v)
}

/// Filtert Overrides, deren `key` in der gegebenen `ColumnMeta`-Liste nicht
/// vorkommt — E1 aus der Spec. Wird vom GraphQL-Resolver vor der Auslieferung
/// auf die `properties`-Liste angewendet.
pub fn strip_unknown_keys(
    properties: &mut Vec<PropertySettings>,
    known_keys: &[String],
    entity_type: &str,
    view_name:   &str,
) {
    let before = properties.len();
    properties.retain(|p| known_keys.iter().any(|k| k == &p.key));
    let dropped = before - properties.len();
    if dropped > 0 {
        tracing::info!(
            target: "server::views",
            "view '{entity_type}/{view_name}' enthielt {dropped} Override(s) fuer unbekannte Spalten"
        );
    }
}
```

- [ ] **Step 4: Registriere Modul** in `server/src/lib.rs`

Suche `pub mod data;` (etwa Zeile 30). Fuege analog hinzu:

```rust
pub mod views;
```

- [ ] **Step 5: Run tests** — alle 5 Unit-Tests muessen passen.

Run: `cargo test -p server --lib views::tests --target-dir target-test`
Expected: PASS — 5 Tests gruen.

- [ ] **Step 6: Commit**

```bash
git add server/src/views.rs server/src/lib.rs
git commit -m "feat(server): merge_layers + resolve_view + strip_unknown_keys (Q0005 D1)"
```

---

## Phase E — Server: GraphQL-API

### Task E1: Neue GraphQL-Types + Query/Mutation-Signaturen

**Files:**
- Modify: `server/src/schema.rs` (Types am Ende der Datei vor `mod tests`; Resolver in `QueryRoot`/`MutationRoot`)

- [ ] **Step 1: Definiere die GraphQL-Types**

Am Ende von `server/src/schema.rs` (vor `mod tests`) einfuegen:

```rust
// =============================================================================
// Q0005 — Named Views
// =============================================================================

#[derive(async_graphql::Enum, Copy, Clone, PartialEq, Eq, Debug)]
#[graphql(name = "ViewLayer")]
pub enum GqlViewLayer { Global, Group, User }

impl From<GqlViewLayer> for shared::view::ViewLayer {
    fn from(v: GqlViewLayer) -> Self {
        match v {
            GqlViewLayer::Global => shared::view::ViewLayer::Global,
            GqlViewLayer::Group  => shared::view::ViewLayer::Group,
            GqlViewLayer::User   => shared::view::ViewLayer::User,
        }
    }
}
impl From<shared::view::ViewLayer> for GqlViewLayer {
    fn from(v: shared::view::ViewLayer) -> Self {
        match v {
            shared::view::ViewLayer::Global => GqlViewLayer::Global,
            shared::view::ViewLayer::Group  => GqlViewLayer::Group,
            shared::view::ViewLayer::User   => GqlViewLayer::User,
        }
    }
}

#[derive(async_graphql::SimpleObject, Clone)]
pub struct EntityViewGql {
    pub id:                String,
    pub entity_type:       String,
    pub view_name:         String,
    pub layer:             GqlViewLayer,
    pub owner_id:          Option<String>,
    /// Sparse Property-Overrides als JSON-Blob — analog zu ColumnMeta.fieldType.
    pub properties:        async_graphql::Json<serde_json::Value>,
    pub default_filter:    Option<async_graphql::Json<serde_json::Value>>,
    pub default_sort:      Option<async_graphql::Json<serde_json::Value>>,
    pub default_page_size: Option<u32>,
    pub version:           i32,
    pub updated_at:        String,
    pub updated_by:        Option<String>,
}

impl From<shared::view::EntityView> for EntityViewGql {
    fn from(v: shared::view::EntityView) -> Self {
        EntityViewGql {
            id:                v.id,
            entity_type:       v.entity_type,
            view_name:         v.view_name,
            layer:             v.layer.into(),
            owner_id:          v.owner_id,
            properties:        async_graphql::Json(serde_json::to_value(v.properties).unwrap_or(serde_json::Value::Null)),
            default_filter:    v.default_filter.map(|f| async_graphql::Json(serde_json::to_value(f).unwrap_or(serde_json::Value::Null))),
            default_sort:      v.default_sort.map(|s| async_graphql::Json(serde_json::to_value(s).unwrap_or(serde_json::Value::Null))),
            default_page_size: v.default_page_size,
            version:           v.version,
            updated_at:        v.updated_at,
            updated_by:        v.updated_by,
        }
    }
}

#[derive(async_graphql::SimpleObject)]
pub struct EntityViewSummaryGql {
    pub view_name:  String,
    pub layers:     Vec<GqlViewLayer>,
    pub updated_at: String,
}

#[derive(async_graphql::InputObject)]
pub struct SaveEntityViewInput {
    pub entity_type:      String,
    pub view_name:        String,
    pub layer:            GqlViewLayer,
    pub owner_id:         Option<String>,
    /// JSON-Blob `{ properties, defaultFilter, defaultSort, defaultPageSize }`
    pub payload:          async_graphql::Json<serde_json::Value>,
    pub expected_version: Option<i32>,
}

#[derive(async_graphql::Enum, Copy, Clone, PartialEq, Eq, Debug)]
pub enum SaveEntityViewResultKind { Ok, Conflict, Forbidden }

#[derive(async_graphql::SimpleObject)]
pub struct SaveEntityViewResult {
    pub kind:    SaveEntityViewResultKind,
    pub view:    Option<EntityViewGql>,
    pub message: Option<String>,
}

#[derive(async_graphql::SimpleObject)]
pub struct RevertEntityViewResult {
    pub ok:      bool,
    pub message: Option<String>,
}
```

- [ ] **Step 2: Resolver in `QueryRoot` einbauen**

Suche im `#[Object] impl QueryRoot { ... }`-Block (in derselben Datei) das Ende und fuege folgende Methoden hinzu:

```rust
async fn entity_view(
    &self,
    ctx: &async_graphql::Context<'_>,
    entity_type: String,
    #[graphql(default = "default")]
    view_name: String,
) -> EntityViewGql {
    use crate::AuthContext;
    let auth = ctx.data_opt::<AuthContext>();
    let user_ref = auth.and_then(|a| a.user.as_ref());
    let resolved = crate::views::resolve_view(&entity_type, &view_name, user_ref).await;
    // ResolvedView -> EntityViewGql shim: wir verpacken die effektive
    // PropertySettings-Liste in der `properties`-JSON-Spalte. Der Client
    // erwartet `Vec<PropertySettings>`.
    EntityViewGql {
        id:                format!("resolved:{entity_type}:{view_name}"),
        entity_type:       resolved.entity_type,
        view_name:         resolved.view_name,
        layer:             GqlViewLayer::Global, // resolved view ist kein einzelner Layer
        owner_id:          None,
        properties:        async_graphql::Json(serde_json::to_value(resolved.properties).unwrap_or(serde_json::Value::Null)),
        default_filter:    resolved.default_filter.map(|f| async_graphql::Json(serde_json::to_value(f).unwrap_or(serde_json::Value::Null))),
        default_sort:      resolved.default_sort.map(|s| async_graphql::Json(serde_json::to_value(s).unwrap_or(serde_json::Value::Null))),
        default_page_size: resolved.default_page_size,
        version:           resolved.provenance.iter().map(|p| p.version).sum::<i32>(),
        updated_at:        String::new(),
        updated_by:        None,
    }
}

async fn entity_views(
    &self,
    _ctx: &async_graphql::Context<'_>,
    entity_type: String,
) -> Vec<EntityViewSummaryGql> {
    crate::data::find_entity_views(&entity_type).await
        .unwrap_or_default()
        .into_iter()
        .map(|s| EntityViewSummaryGql {
            view_name:  s.view_name,
            layers:     s.layers.into_iter().map(GqlViewLayer::from).collect(),
            updated_at: s.updated_at,
        })
        .collect()
}
```

- [ ] **Step 3: Resolver in `MutationRoot` einbauen**

Im `#[Object] impl MutationRoot { ... }`-Block:

```rust
async fn save_entity_view(
    &self,
    ctx: &async_graphql::Context<'_>,
    input: SaveEntityViewInput,
) -> SaveEntityViewResult {
    use crate::AuthContext;
    let auth = ctx.data_opt::<AuthContext>();
    let Some(user) = auth.and_then(|a| a.user.as_ref()) else {
        return SaveEntityViewResult {
            kind: SaveEntityViewResultKind::Forbidden, view: None,
            message: Some("nicht authentifiziert".into()),
        };
    };
    if !shared::is_allowed(user, &crate::data::groups().await, &input.entity_type, shared::PermissionOp::Update) {
        return SaveEntityViewResult {
            kind: SaveEntityViewResultKind::Forbidden, view: None,
            message: Some(format!("kein Update-Recht auf '{}'", input.entity_type)),
        };
    }

    // Layer-Owner-Invariante.
    let layer: shared::view::ViewLayer = input.layer.into();
    let owner_id = input.owner_id.clone();
    let invariant_ok = (layer == shared::view::ViewLayer::Global && owner_id.is_none())
        || (layer != shared::view::ViewLayer::Global && owner_id.is_some());
    if !invariant_ok {
        return SaveEntityViewResult {
            kind: SaveEntityViewResultKind::Forbidden, view: None,
            message: Some("layer=global requires owner_id=null and vice versa".into()),
        };
    }

    // Aktuelle Version laden -> Conflict-Check.
    let existing = crate::data::find_entity_view(
        &input.entity_type, &input.view_name, layer, owner_id.as_deref()
    ).await.unwrap_or(None);
    if let (Some(curr), Some(expected)) = (existing.as_ref(), input.expected_version) {
        if curr.version != expected {
            return SaveEntityViewResult {
                kind: SaveEntityViewResultKind::Conflict,
                view: Some(curr.clone().into()),
                message: Some(format!("expected_version={expected}, current={}", curr.version)),
            };
        }
    }

    // Payload entpacken.
    #[derive(serde::Deserialize)]
    #[serde(rename_all = "camelCase")]
    struct PayloadIn {
        properties: Vec<shared::view::ViewPropertyOverride>,
        #[serde(default)] default_filter:    Option<shared::FilterCriteria>,
        #[serde(default)] default_sort:      Option<shared::Sort>,
        #[serde(default)] default_page_size: Option<u32>,
    }
    let p: PayloadIn = match serde_json::from_value(input.payload.0) {
        Ok(p)  => p,
        Err(e) => return SaveEntityViewResult {
            kind: SaveEntityViewResultKind::Forbidden, view: None,
            message: Some(format!("payload-parse: {e}")),
        },
    };

    let new_version = existing.as_ref().map(|v| v.version + 1).unwrap_or(1);
    let id = existing.as_ref().map(|v| v.id.clone())
        .unwrap_or_else(|| format!("v-{}", uuid::Uuid::new_v4()));
    let v = shared::view::EntityView {
        id,
        entity_type: input.entity_type.clone(),
        view_name:   input.view_name.clone(),
        layer,
        owner_id,
        properties:        p.properties,
        default_filter:    p.default_filter,
        default_sort:      p.default_sort,
        default_page_size: p.default_page_size,
        version: new_version,
        updated_at: chrono::Utc::now().to_rfc3339(),
        updated_by: Some(user.id.clone()),
    };
    if let Err(e) = crate::data::upsert_entity_view(&v).await {
        return SaveEntityViewResult {
            kind: SaveEntityViewResultKind::Forbidden, view: None,
            message: Some(format!("save: {e}")),
        };
    }
    SaveEntityViewResult {
        kind: SaveEntityViewResultKind::Ok,
        view: Some(v.into()),
        message: None,
    }
}

async fn revert_entity_view(
    &self,
    ctx: &async_graphql::Context<'_>,
    entity_type: String,
    view_name:   String,
    layer:       GqlViewLayer,
    owner_id:    Option<String>,
) -> RevertEntityViewResult {
    use crate::AuthContext;
    let auth = ctx.data_opt::<AuthContext>();
    let Some(user) = auth.and_then(|a| a.user.as_ref()) else {
        return RevertEntityViewResult { ok: false, message: Some("unauthenticated".into()) };
    };
    if !shared::is_allowed(user, &crate::data::groups().await, &entity_type, shared::PermissionOp::Update) {
        return RevertEntityViewResult { ok: false, message: Some("forbidden".into()) };
    }
    match crate::data::delete_entity_view(&entity_type, &view_name, layer.into(), owner_id.as_deref()).await {
        Ok(_)  => RevertEntityViewResult { ok: true, message: None },
        Err(e) => RevertEntityViewResult { ok: false, message: Some(e) },
    }
}
```

- [ ] **Step 2.5: Dependency check**

Pruefe `Cargo.toml` von `server`:
- `uuid` — falls nicht vorhanden, in `[dependencies]` ergaenzen:
  ```toml
  uuid = { version = "1", features = ["v4"] }
  ```
- `chrono` — vermutlich schon da (sonst analog ergaenzen mit `features = ["clock"]`).

Run: `cargo check -p server` — Erwartung: Compile.

- [ ] **Step 3: Smoke-Compile**

Run: `cargo check -p server --target-dir target-test`
Expected: Compile sauber, keine Errors. (Warnings durch unused-imports okay.)

- [ ] **Step 4: Commit**

```bash
git add server/src/schema.rs server/Cargo.toml
git commit -m "feat(server): GraphQL entityView/entityViews + saveEntityView/revertEntityView (Q0005 E1)"
```

### Task E2: Rewire `entitySettings`-Query auf den Resolver

**Files:**
- Modify: `server/src/schema.rs` (existierender `entity_settings`-Resolver)

- [ ] **Step 1: Finde den bestehenden Resolver**

Run: `rg -n "fn entity_settings" server/src/schema.rs`
Erwartung: 1 Treffer.

- [ ] **Step 2: Patchen — Body durch resolve_view-Aufruf ersetzen**

Der bestehende Body liest aktuell direkt aus `data::entity_settings`. Ersetze ihn durch:

```rust
async fn entity_settings(
    &self,
    ctx: &async_graphql::Context<'_>,
    entity_type: String,
) -> Option<async_graphql::Json<serde_json::Value>> {
    use crate::AuthContext;
    let auth = ctx.data_opt::<AuthContext>();
    let user_ref = auth.and_then(|a| a.user.as_ref());
    let resolved = crate::views::resolve_view(&entity_type, "default", user_ref).await;
    // Wenn keine Layer vorliegen, ist provenance leer und properties leer —
    // gib dann None zurueck, damit der Client auf Default-Verhalten faellt.
    if resolved.provenance.is_empty() {
        return None;
    }
    let settings = shared::EntitySettings {
        entity_type:         resolved.entity_type,
        access:              shared::Access::default(),
        default_page_size:   resolved.default_page_size,
        default_sort:        resolved.default_sort,
        default_filter:      resolved.default_filter,
        properties:          resolved.properties,
        field_type_defaults: shared::FieldTypeDefaults::default(),
    };
    Some(async_graphql::Json(serde_json::to_value(settings).unwrap_or(serde_json::Value::Null)))
}
```

(Falls die bestehende Signatur leicht abweicht — gleichen Namen + Return-Typ uebernehmen; **kein** Wire-Bruch.)

- [ ] **Step 3: Bestehende E2E-Tests laufen lassen**

Run: `cargo test -p server --target-dir target-test`
Expected: PASS — bestehende Tests duerfen nicht regredieren. Wenn ein Test
`entity_settings` aufruft und 0 Rows erwartet (weil Loader noch nicht in
`entity_views` gespiegelt ist), markiere ihn `#[ignore]` mit Begruendung
und plane die Reparatur in Task F1 ein.

- [ ] **Step 4: Commit**

```bash
git add server/src/schema.rs
git commit -m "refactor(server): entitySettings resolves via merge_layers (Q0005 E2)"
```

---

## Phase F — Server: Loader-Bootstrap

### Task F1: `seed_entity_views_from_example`

**Files:**
- Modify: `server/src/data.rs` (neue Funktion + Aufruf in `db.rs`)
- Modify: `server/src/db.rs` (in `seed_if_empty` aufrufen)

- [ ] **Step 1: Schreibe Test** in `server/tests/entity_views.rs` (anhaengen)

```rust
#[tokio::test]
#[serial_test::serial]
async fn loader_bootstrap_creates_default_global_view_per_entity_type_with_settings() {
    let _ = server::fresh_test_setup().await;
    // setup_for_tests laedt 'shop'-Beispiel; pruefen wir auf 'product' (hat settings).
    let row = data::find_entity_view("product", "default", ViewLayer::Global, None)
        .await
        .expect("find");
    assert!(row.is_some(), "Loader-Settings fuer 'product' werden zur Default-View gemacht");
    let v = row.unwrap();
    assert_eq!(v.version, 0);
    assert_eq!(v.updated_by.as_deref(), Some("system"));
}

#[tokio::test]
#[serial_test::serial]
async fn loader_bootstrap_is_idempotent() {
    let _ = server::fresh_test_setup().await;
    // Manuell nochmal aufrufen; bestehende Row bleibt unangetastet (version=0).
    server::data::seed_entity_views_from_example(&server::db::conn()).await.unwrap();
    let row = data::find_entity_view("product", "default", ViewLayer::Global, None)
        .await.unwrap().unwrap();
    assert_eq!(row.version, 0, "idempotent");
}
```

- [ ] **Step 2: Run tests** — Erwartung: FAIL (Funktion existiert noch nicht).

Run: `cargo test -p server --test entity_views --target-dir target-test`
Expected: FAIL — `seed_entity_views_from_example` unbekannt.

- [ ] **Step 3: Implementiere in `server/src/data.rs`**

Am Ende der Datei (nach `delete_entity_view`):

```rust
/// Phase F (Q0005): Loader-Bootstrap. Pro Entity-Typ aus dem aktuellen
/// `ExampleSet` mit gesetztem `EntitySettings` legen wir eine
/// `(view_name="default", layer="global", owner_id=NULL)`-Row an.
/// Idempotent — bestehende Rows bleiben unangetastet.
pub async fn seed_entity_views_from_example(db: &sea_orm::DatabaseConnection) -> Result<(), sea_orm::DbErr> {
    let Some(set) = crate::example::current() else { return Ok(()); };
    for (entity_type, ty_set) in &set.entities {
        let Some(settings) = ty_set.settings.as_ref() else { continue; };
        // Existiert schon? -> skip
        let existing = find_entity_view(entity_type, "default", shared::view::ViewLayer::Global, None)
            .await
            .map_err(|e| sea_orm::DbErr::Custom(e))?;
        if existing.is_some() { continue; }

        // Aus EntitySettings → ViewPropertyOverride mappen.
        let properties: Vec<shared::view::ViewPropertyOverride> = settings.properties.iter().map(|p| {
            shared::view::ViewPropertyOverride {
                key: p.key.clone(),
                visibility: Some(p.visibility),
                order: Some(p.order),
                min_width: p.min_width,
                label_override_key: p.label_override_key.clone(),
                sortable: None,
                filter_id_override: None,
                formatter_id_override: None,
            }
        }).collect();

        let view = shared::view::EntityView {
            id: format!("v-system-{entity_type}-default-global"),
            entity_type: entity_type.clone(),
            view_name: "default".into(),
            layer: shared::view::ViewLayer::Global,
            owner_id: None,
            properties,
            default_filter:    settings.default_filter.clone(),
            default_sort:      settings.default_sort.clone(),
            default_page_size: settings.default_page_size,
            version: 0,
            updated_at: chrono::Utc::now().to_rfc3339(),
            updated_by: Some("system".into()),
        };
        upsert_entity_view(&view).await.map_err(sea_orm::DbErr::Custom)?;
    }
    Ok(())
}
```

- [ ] **Step 4: Hook in `seed_if_empty`** in `server/src/db.rs`

Am Ende von `seed_if_empty()` (vor `Ok(())`) einfuegen:

```rust
// Phase F (Q0005): Loader-Settings als Default-Global-View materialisieren.
crate::data::seed_entity_views_from_example(db).await?;
```

- [ ] **Step 5: Tests laufen lassen**

Run: `cargo test -p server --test entity_views --target-dir target-test`
Expected: PASS — alle Tests inkl. neuen 2 Loader-Tests.

- [ ] **Step 6: Commit**

```bash
git add server/src/data.rs server/src/db.rs
git commit -m "feat(server): seed default global views from example loader (Q0005 F1)"
```

---

## Phase G — Server: E2E-GraphQL-Tests

### Task G1: GraphQL-E2E fuer entity_view + saveEntityView + revertEntityView

**Files:**
- Modify: `server/tests/e2e.rs` (anhaengen — neue `#[tokio::test]` mit `#[serial_test::serial]`)

- [ ] **Step 1: Schreibe Test (5 Faelle)** am Ende von `server/tests/e2e.rs`:

```rust
// =============================================================================
// Q0005: Named Views E2E
// =============================================================================

#[tokio::test]
#[serial_test::serial]
async fn entity_view_returns_resolved_default_for_authenticated_user() {
    let schema = server::fresh_test_setup().await;
    // Login als shop-admin (passwort 'admin' im shop-Beispiel).
    let token = login_helper(&schema, "admin", "admin").await;
    let q = r#"query { entityView(entityType: "product") { entityType viewName version } }"#;
    let res = exec_with_token(&schema, q, &token).await;
    let v = res["data"]["entityView"].as_object().expect("entityView");
    assert_eq!(v["entityType"], "product");
    assert_eq!(v["viewName"], "default");
}

#[tokio::test]
#[serial_test::serial]
async fn save_entity_view_creates_then_conflicts_on_stale_version() {
    let schema = server::fresh_test_setup().await;
    let token = login_helper(&schema, "admin", "admin").await;

    let create = r#"mutation { saveEntityView(input: {
        entityType: "product", viewName: "default", layer: GLOBAL,
        payload: { properties: [], defaultPageSize: 50 }
    }) { kind view { version } message } }"#;
    let res = exec_with_token(&schema, create, &token).await;
    let kind = res["data"]["saveEntityView"]["kind"].as_str().unwrap();
    assert_eq!(kind, "OK");

    let stale = r#"mutation { saveEntityView(input: {
        entityType: "product", viewName: "default", layer: GLOBAL,
        expectedVersion: 0,
        payload: { properties: [] }
    }) { kind } }"#;
    let res2 = exec_with_token(&schema, stale, &token).await;
    assert_eq!(res2["data"]["saveEntityView"]["kind"].as_str().unwrap(), "CONFLICT");
}

#[tokio::test]
#[serial_test::serial]
async fn save_entity_view_forbidden_without_auth() {
    let schema = server::fresh_test_setup().await;
    let m = r#"mutation { saveEntityView(input: {
        entityType: "product", viewName: "default", layer: GLOBAL,
        payload: { properties: [] }
    }) { kind message } }"#;
    let res = exec_anonymous(&schema, m).await;
    assert_eq!(res["data"]["saveEntityView"]["kind"].as_str().unwrap(), "FORBIDDEN");
}

#[tokio::test]
#[serial_test::serial]
async fn revert_entity_view_removes_user_layer_only() {
    let schema = server::fresh_test_setup().await;
    let token = login_helper(&schema, "admin", "admin").await;
    // Save User-Layer
    let save = r#"mutation { saveEntityView(input: {
        entityType: "product", viewName: "default", layer: USER, ownerId: "u-admin",
        payload: { properties: [] }
    }) { kind } }"#;
    let _ = exec_with_token(&schema, save, &token).await;

    // Revert User-Layer
    let revert = r#"mutation { revertEntityView(entityType: "product", viewName: "default", layer: USER, ownerId: "u-admin") { ok } }"#;
    let r = exec_with_token(&schema, revert, &token).await;
    assert!(r["data"]["revertEntityView"]["ok"].as_bool().unwrap());

    // Global bleibt
    let q = r#"query { entityView(entityType: "product") { viewName version } }"#;
    let res = exec_with_token(&schema, q, &token).await;
    assert_eq!(res["data"]["entityView"]["viewName"], "default");
}

#[tokio::test]
#[serial_test::serial]
async fn entity_settings_resolves_via_view_overlay() {
    let schema = server::fresh_test_setup().await;
    let token = login_helper(&schema, "admin", "admin").await;
    // Default-View existiert aus Loader-Seed.
    let q = r#"query { entitySettings(entityType: "product") }"#;
    let res = exec_with_token(&schema, q, &token).await;
    // entitySettings ist async_graphql::Json<Value>; serialisiert als JSON-Objekt.
    assert!(res["data"]["entitySettings"].is_object() || res["data"]["entitySettings"].is_null());
}
```

> Anmerkung: `login_helper`, `exec_with_token`, `exec_anonymous` sind hypothetische Helper. Pruefe im bestehenden `server/tests/e2e.rs`, welche Helper schon existieren (Run: `rg -n "fn login_helper|fn exec" server/tests/e2e.rs`). Wenn sie nicht existieren, baue sie analog zum bestehenden Login-Test-Muster (siehe `auth_login_returns_session_token` o.aehnl.).

- [ ] **Step 2: Tests laufen lassen, Fehler iterieren**

Run: `cargo test -p server --test e2e --target-dir target-test`
Expected: PASS — alle 5 neuen Tests + bestehende.

Falls Helper fehlen, vorher anlegen (Step 1a):

```rust
async fn exec_anonymous(schema: &server::AppSchema, q: &str) -> serde_json::Value {
    let req = async_graphql::Request::new(q).data(server::AuthContext { user: None, token: None });
    let resp = schema.execute(req).await;
    serde_json::to_value(&resp).unwrap()
}

async fn exec_with_token(schema: &server::AppSchema, q: &str, token: &str) -> serde_json::Value {
    let user = server::auth::user_for_bearer(Some(&format!("Bearer {token}"))).await;
    let req = async_graphql::Request::new(q).data(server::AuthContext { user, token: Some(token.into()) });
    let resp = schema.execute(req).await;
    serde_json::to_value(&resp).unwrap()
}

async fn login_helper(schema: &server::AppSchema, username: &str, password: &str) -> String {
    let m = format!(r#"mutation {{ login(username: "{username}", password: "{password}") {{ session {{ token }} }} }}"#);
    let r = exec_anonymous(schema, &m).await;
    r["data"]["login"]["session"]["token"].as_str().unwrap().to_string()
}
```

- [ ] **Step 3: Commit**

```bash
git add server/tests/e2e.rs
git commit -m "test(server): GraphQL e2e for entityView / saveEntityView / revertEntityView (Q0005 G1)"
```

---

## Phase H — Client: GraphQL-Adapter

### Task H1: 4 neue Adapter-Funktionen

**Files:**
- Modify: `client/src/graphql/queries.rs` (am Ende einfuegen)

- [ ] **Step 1: Konstanten + Structs**

Am Ende von `client/src/graphql/queries.rs`:

```rust
// =============================================================================
// Q0005 — Named Views
// =============================================================================

const ENTITY_VIEW_QUERY: &str = r#"
    query EntityView($entityType: String!, $viewName: String!) {
        entityView(entityType: $entityType, viewName: $viewName) {
            id entityType viewName layer ownerId
            properties defaultFilter defaultSort defaultPageSize
            version updatedAt updatedBy
        }
    }
"#;

const ENTITY_VIEWS_QUERY: &str = r#"
    query EntityViews($entityType: String!) {
        entityViews(entityType: $entityType) { viewName layers updatedAt }
    }
"#;

const SAVE_ENTITY_VIEW_MUTATION: &str = r#"
    mutation SaveEntityView($input: SaveEntityViewInput!) {
        saveEntityView(input: $input) {
            kind message
            view { id entityType viewName layer ownerId properties defaultFilter defaultSort defaultPageSize version updatedAt updatedBy }
        }
    }
"#;

const REVERT_ENTITY_VIEW_MUTATION: &str = r#"
    mutation RevertEntityView($entityType: String!, $viewName: String!, $layer: ViewLayer!, $ownerId: String) {
        revertEntityView(entityType: $entityType, viewName: $viewName, layer: $layer, ownerId: $ownerId) { ok message }
    }
"#;

#[derive(serde::Serialize)]
struct EntityTypeVar<'a> { entity_type: &'a str }

#[derive(serde::Serialize)]
struct EntityViewVars<'a> { entity_type: &'a str, view_name: &'a str }

#[derive(serde::Deserialize)]
struct EntityViewResp { #[serde(rename = "entityView")] entity_view: Option<RawEntityView> }

#[derive(serde::Deserialize)]
struct EntityViewsResp { #[serde(rename = "entityViews")] entity_views: Vec<shared::view::ResolvedLayerRef> }
// NB: kleine Abkuerzung — der Server liefert EntityViewSummaryGql, das wir
// im Client als locally-gespiegelten Typ deserialisieren.

#[derive(serde::Deserialize)]
struct RawEntityView {
    id: String,
    #[serde(rename = "entityType")] entity_type: String,
    #[serde(rename = "viewName")]   view_name:   String,
    layer:    shared::view::ViewLayer,
    #[serde(rename = "ownerId")]    owner_id:    Option<String>,
    properties:        serde_json::Value,
    #[serde(rename = "defaultFilter")]   default_filter:    Option<serde_json::Value>,
    #[serde(rename = "defaultSort")]     default_sort:      Option<serde_json::Value>,
    #[serde(rename = "defaultPageSize")] default_page_size: Option<u32>,
    version:    i32,
    #[serde(rename = "updatedAt")] updated_at: String,
    #[serde(rename = "updatedBy")] updated_by: Option<String>,
}

impl From<RawEntityView> for shared::view::EntityView {
    fn from(r: RawEntityView) -> Self {
        shared::view::EntityView {
            id: r.id, entity_type: r.entity_type, view_name: r.view_name,
            layer: r.layer, owner_id: r.owner_id,
            properties: serde_json::from_value(r.properties).unwrap_or_default(),
            default_filter: r.default_filter.and_then(|v| serde_json::from_value(v).ok()),
            default_sort:   r.default_sort.and_then(|v| serde_json::from_value(v).ok()),
            default_page_size: r.default_page_size,
            version: r.version, updated_at: r.updated_at, updated_by: r.updated_by,
        }
    }
}

pub async fn fetch_entity_view(entity_type: &str, view_name: &str)
    -> Result<Option<shared::view::EntityView>, super::GqlError>
{
    let d: EntityViewResp = super::execute(ENTITY_VIEW_QUERY, EntityViewVars { entity_type, view_name }).await?;
    Ok(d.entity_view.map(Into::into))
}

#[derive(serde::Serialize)]
pub struct SaveEntityViewInputClient<'a> {
    pub entity_type:      &'a str,
    pub view_name:        &'a str,
    pub layer:            shared::view::ViewLayer,
    pub owner_id:         Option<&'a str>,
    pub payload:          serde_json::Value,
    pub expected_version: Option<i32>,
}

#[derive(serde::Deserialize)]
pub struct SaveEntityViewOutcomeClient {
    pub kind:    String,
    pub message: Option<String>,
    pub view:    Option<RawEntityView>,
}

#[derive(serde::Serialize)]
struct SaveVars<'a> { input: SaveEntityViewInputClient<'a> }

#[derive(serde::Deserialize)]
struct SaveResp { #[serde(rename = "saveEntityView")] save_entity_view: SaveEntityViewOutcomeClient }

pub async fn save_entity_view(input: SaveEntityViewInputClient<'_>) -> Result<SaveEntityViewOutcomeClient, super::GqlError> {
    let r: SaveResp = super::execute(SAVE_ENTITY_VIEW_MUTATION, SaveVars { input }).await?;
    Ok(r.save_entity_view)
}

#[derive(serde::Serialize)]
struct RevertVars<'a> {
    entity_type: &'a str, view_name: &'a str,
    layer: shared::view::ViewLayer, owner_id: Option<&'a str>,
}

#[derive(serde::Deserialize)]
struct RevertResp { #[serde(rename = "revertEntityView")] revert_entity_view: RevertEntityViewOutcomeClient }

#[derive(serde::Deserialize)]
pub struct RevertEntityViewOutcomeClient { pub ok: bool, pub message: Option<String> }

pub async fn revert_entity_view(
    entity_type: &str, view_name: &str,
    layer: shared::view::ViewLayer, owner_id: Option<&str>,
) -> Result<RevertEntityViewOutcomeClient, super::GqlError> {
    let r: RevertResp = super::execute(
        REVERT_ENTITY_VIEW_MUTATION,
        RevertVars { entity_type, view_name, layer, owner_id },
    ).await?;
    Ok(r.revert_entity_view)
}
```

- [ ] **Step 2: Compile-Check**

Run: `cargo check -p client --target wasm32-unknown-unknown`
Expected: kein Fehler.

- [ ] **Step 3: Commit**

```bash
git add client/src/graphql/queries.rs
git commit -m "feat(client): GraphQL adapters for entityView/saveEntityView/revertEntityView (Q0005 H1)"
```

---

## Phase I — Client: Pure-Function-Layer

### Task I1: `apply_pending_overrides`

**Files:**
- Create: `client/src/components/table/column_editor/mod.rs`
- Modify: `client/src/components/table/mod.rs` (`pub mod column_editor;`)

- [ ] **Step 1: Modul registrieren**

In `client/src/components/table/mod.rs` (suche `pub mod ...`-Block am Anfang) ergaenzen:
```rust
pub mod column_editor;
pub use column_editor::{apply_pending_overrides, compute_reorder};
```

- [ ] **Step 2: Schreibe Test** am Ende von `client/src/components/table/column_editor/mod.rs`

```rust
//! Q0005 — Pure-Function-Layer fuer den In-Place Column-Editor.

use std::collections::HashMap;
use shared::view::ViewPropertyOverride;
use shared::{ColumnMeta, EntitySettings, PropertySettings, Visibility};

/// Wendet pending Edits auf `cols` + `settings` an. Beide werden in-place
/// mutiert; das Rendering liest hinterher die geaenderten Werte.
pub fn apply_pending_overrides(
    cols:      &mut Vec<ColumnMeta>,
    settings:  &mut EntitySettings,
    overrides: &HashMap<String, ViewPropertyOverride>,
) {
    for (key, ov) in overrides {
        // ColumnMeta-Felder (sortable/filter_id/formatter_id) direkt
        if let Some(col) = cols.iter_mut().find(|c| &c.key == key) {
            if let Some(s)  = ov.sortable                                { col.sortable    = s; }
            if let Some(id) = ov.filter_id_override.as_deref()           { col.filter_id    = Some(id.into()); }
            if let Some(id) = ov.formatter_id_override.as_deref()        { col.formatter_id = Some(id.into()); }
        }
        // PropertySettings-Felder
        let p = settings.ensure_property(key);
        if let Some(v) = ov.visibility                  { p.visibility         = v; }
        if let Some(o) = ov.order                       { p.order              = o; }
        if let Some(w) = ov.min_width                   { p.min_width          = Some(w); }
        if let Some(l) = ov.label_override_key.clone()  { p.label_override_key = Some(l); }
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use shared::view::ViewLayer;

    fn col(key: &str) -> ColumnMeta {
        ColumnMeta {
            key: key.into(),
            label_key: format!("col.{key}"),
            field_type: shared::FieldType::Text,
            sortable: false, filterable: false,
            comparator_id: None, filter_id: None,
            editor_id: None, formatter_id: None,
            action_ids: Vec::new(),
        }
    }

    #[test]
    fn applies_sortable_override_to_column_meta() {
        let mut cols = vec![col("amount")];
        let mut settings = EntitySettings::default();
        let mut ovs = HashMap::new();
        ovs.insert("amount".into(), ViewPropertyOverride {
            key: "amount".into(),
            sortable: Some(true),
            ..Default::default()
        });
        apply_pending_overrides(&mut cols, &mut settings, &ovs);
        assert!(cols[0].sortable);
    }

    #[test]
    fn applies_visibility_to_settings() {
        let mut cols = vec![col("amount")];
        let mut settings = EntitySettings::default();
        let mut ovs = HashMap::new();
        ovs.insert("amount".into(), ViewPropertyOverride {
            key: "amount".into(),
            visibility: Some(Visibility::Hidden),
            ..Default::default()
        });
        apply_pending_overrides(&mut cols, &mut settings, &ovs);
        assert_eq!(settings.property("amount").unwrap().visibility, Visibility::Hidden);
    }

    #[test]
    fn unknown_keys_are_silently_ignored() {
        let mut cols = vec![col("amount")];
        let mut settings = EntitySettings::default();
        let mut ovs = HashMap::new();
        ovs.insert("foreign".into(), ViewPropertyOverride {
            key: "foreign".into(),
            sortable: Some(true),
            ..Default::default()
        });
        apply_pending_overrides(&mut cols, &mut settings, &ovs);
        assert!(!cols[0].sortable, "amount unangetastet");
        // PropertySettings wird angelegt — das ist okay, der Renderer filtert
        // sowieso auf cols.
        assert!(settings.property("foreign").is_some());
    }
}
```

- [ ] **Step 3: Run tests**

Run: `cargo test -p client --lib column_editor::tests`
Expected: PASS — 3 Tests gruen.

- [ ] **Step 4: Commit**

```bash
git add client/src/components/table/column_editor/ client/src/components/table/mod.rs
git commit -m "feat(client): apply_pending_overrides pure function + tests (Q0005 I1)"
```

### Task I2: `compute_reorder` fuer Drag-Reorder am Header

**Files:**
- Modify: `client/src/components/table/column_editor/mod.rs` (anhaengen)

- [ ] **Step 1: Schreibe Test** in `client/src/components/table/column_editor/mod.rs`

```rust
/// Header-Bounding-Box im Viewport (px).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct HeaderRect {
    pub left:   f64,
    pub right:  f64,
}

#[derive(Debug, Clone, Copy)]
pub struct DragState {
    pub from_index: usize,
    pub pointer_x:  f64,
}

/// Berechnet die neue Spalten-Reihenfolge als Indexliste. Stable.
///
/// Inputs:
///   - `headers`: aktuelle Header-Rects in Render-Reihenfolge
///   - `drag`:    welche Spalte gerade gezogen wird + aktuelle Maus-X
///
/// Output: Vec<usize> der gleichen Laenge wie `headers`, jeweils Index in
/// der urspruenglichen Liste. Beispiel: [0,1,2] -> dragged 0 nach Mitte
/// von 2 -> [1,2,0].
pub fn compute_reorder(headers: &[HeaderRect], drag: &DragState) -> Vec<usize> {
    let n = headers.len();
    if n == 0 || drag.from_index >= n { return (0..n).collect(); }
    // Drop-Zone: ueber welchem Header ist die Maus?
    let to_index = headers.iter().enumerate()
        .find(|(_, r)| drag.pointer_x >= r.left && drag.pointer_x < r.right)
        .map(|(i, _)| i)
        .unwrap_or(if drag.pointer_x < headers[0].left { 0 } else { n - 1 });
    let mut idx: Vec<usize> = (0..n).collect();
    let item = idx.remove(drag.from_index);
    let insert = if to_index > drag.from_index { to_index } else { to_index };
    let insert = insert.min(idx.len());
    idx.insert(insert, item);
    idx
}

#[cfg(test)]
mod reorder_tests {
    use super::*;
    fn r(l: f64) -> HeaderRect { HeaderRect { left: l, right: l + 100.0 } }

    #[test]
    fn drag_first_to_third_swaps_to_back() {
        let h = vec![r(0.0), r(100.0), r(200.0)];
        let ds = DragState { from_index: 0, pointer_x: 250.0 };
        assert_eq!(compute_reorder(&h, &ds), vec![1, 2, 0]);
    }

    #[test]
    fn drag_last_to_first_swaps_to_front() {
        let h = vec![r(0.0), r(100.0), r(200.0)];
        let ds = DragState { from_index: 2, pointer_x: 50.0 };
        assert_eq!(compute_reorder(&h, &ds), vec![2, 0, 1]);
    }

    #[test]
    fn drag_onto_self_is_noop() {
        let h = vec![r(0.0), r(100.0), r(200.0)];
        let ds = DragState { from_index: 1, pointer_x: 150.0 };
        assert_eq!(compute_reorder(&h, &ds), vec![0, 1, 2]);
    }

    #[test]
    fn pointer_left_of_all_drops_at_index_0() {
        let h = vec![r(0.0), r(100.0)];
        let ds = DragState { from_index: 1, pointer_x: -50.0 };
        assert_eq!(compute_reorder(&h, &ds), vec![1, 0]);
    }
}
```

- [ ] **Step 2: Tests laufen lassen**

Run: `cargo test -p client --lib column_editor`
Expected: PASS — Tests aus I1 + 4 neue.

- [ ] **Step 3: Commit**

```bash
git add client/src/components/table/column_editor/mod.rs
git commit -m "feat(client): compute_reorder pure function + tests (Q0005 I2)"
```

### Task I3: `FilterRegistry::compatible_for` + `FilterDescriptor`

**Files:**
- Modify: `client/src/components/table/filters/registry.rs`

- [ ] **Step 1: Bestand erkunden**

Run: `cat client/src/components/table/filters/registry.rs`
Identifiziere die existierende `FilterRegistry`-Struktur (Trait? Struct? `HashMap<id, factory>`?).

- [ ] **Step 2: Erweitern**

Fuege am Ende der Datei hinzu:

```rust
/// Q0005 — UI-Discovery der pro FieldType passenden Filter.
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct FilterDescriptor {
    pub id:        String,
    pub label_key: String,
}

impl FilterRegistry {
    /// Liefert die fuer den gegebenen `FieldType` registrierten Filter
    /// mit ihrer ID + Fluent-Label-Key. Stabil sortiert nach `id`.
    pub fn compatible_for(&self, ft: &shared::FieldType) -> Vec<FilterDescriptor> {
        use shared::FieldType::*;
        let candidates: &[&str] = match ft {
            Text     => &["contains", "equals"],
            Integer | Decimal { .. } | Money { .. } => &["range", "equals"],
            Boolean  => &["equals"],
            Date | DateTime => &["range"],
            Reference { .. } | Collection { .. } | Enum { .. } => &["equals"],
            _        => &[],
        };
        let mut out: Vec<FilterDescriptor> = candidates.iter()
            .filter_map(|id| self.has_factory(id).then(|| FilterDescriptor {
                id: (*id).into(),
                label_key: format!("filter.{id}"),
            }))
            .collect();
        out.sort_by(|a, b| a.id.cmp(&b.id));
        out
    }
}
```

> Falls `FilterRegistry` kein `has_factory(&str)`-Helper hat, fuege ihn analog zum bestehenden `for_filter_id`-Lookup hinzu (Lookup im internen Storage; gibt `bool` zurueck).

- [ ] **Step 3: Test**

Anhaengen am Dateiende:

```rust
#[cfg(test)]
mod compatible_tests {
    use super::*;
    use shared::FieldType;

    fn reg() -> FilterRegistry { crate::components::table::filters::default_registry() }

    #[test]
    fn money_offers_range_and_equals() {
        let d = reg().compatible_for(&FieldType::Money { currency_code_field: None });
        let ids: Vec<&str> = d.iter().map(|f| f.id.as_str()).collect();
        assert!(ids.contains(&"equals"));
        assert!(ids.contains(&"range"));
    }

    #[test]
    fn text_offers_contains_and_equals() {
        let d = reg().compatible_for(&FieldType::Text);
        let ids: Vec<&str> = d.iter().map(|f| f.id.as_str()).collect();
        assert!(ids.contains(&"contains"));
        assert!(ids.contains(&"equals"));
    }
}
```

- [ ] **Step 4: Run tests**

Run: `cargo test -p client --lib filters::registry`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add client/src/components/table/filters/registry.rs
git commit -m "feat(client): FilterRegistry::compatible_for + FilterDescriptor (Q0005 I3)"
```

### Task I4: `FormatterRegistry::compatible_for`

**Files:**
- Modify: `client/src/components/table/formatters.rs`

- [ ] **Step 1: Bestand erkunden**

Run: `head -80 client/src/components/table/formatters.rs`
Notiere, ob es schon eine Registry oder nur freie Funktionen gibt.

- [ ] **Step 2: Erweitern**

Falls noch keine Registry existiert, am Ende der Datei einfuegen:

```rust
/// Q0005 — UI-Discovery der pro FieldType passenden Formatter.
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct FormatterDescriptor {
    pub id:        String,
    pub label_key: String,
}

pub fn compatible_formatters_for(ft: &shared::FieldType) -> Vec<FormatterDescriptor> {
    use shared::FieldType::*;
    let raw: &[(&str, &str)] = match ft {
        Money { .. } => &[
            ("money-symbol",   "formatter.money.symbol"),
            ("money-code",     "formatter.money.code"),
            ("money-decimals", "formatter.money.decimals"),
        ],
        Decimal { .. } => &[
            ("decimal-default", "formatter.decimal.default"),
            ("decimal-2",       "formatter.decimal.2"),
        ],
        Date => &[
            ("date-iso",   "formatter.date.iso"),
            ("date-local", "formatter.date.local"),
        ],
        DateTime => &[
            ("datetime-iso",   "formatter.datetime.iso"),
            ("datetime-local", "formatter.datetime.local"),
        ],
        Integer  => &[("int-default", "formatter.int.default")],
        Boolean  => &[("bool-yesno",  "formatter.bool.yesno")],
        Text     => &[("text-plain",  "formatter.text.plain")],
        _        => &[],
    };
    raw.iter().map(|(id, key)| FormatterDescriptor { id: (*id).into(), label_key: (*key).into() }).collect()
}

#[cfg(test)]
mod compatible_tests {
    use super::*;
    use shared::FieldType;

    #[test]
    fn money_has_three_formatters() {
        let d = compatible_formatters_for(&FieldType::Money { currency_code_field: None });
        assert_eq!(d.len(), 3);
    }

    #[test]
    fn boolean_has_yesno_formatter() {
        let d = compatible_formatters_for(&FieldType::Boolean);
        assert_eq!(d.iter().map(|x| x.id.as_str()).collect::<Vec<_>>(), vec!["bool-yesno"]);
    }
}
```

- [ ] **Step 3: Run tests**

Run: `cargo test -p client --lib formatters`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add client/src/components/table/formatters.rs
git commit -m "feat(client): compatible_formatters_for + FormatterDescriptor (Q0005 I4)"
```

---

## Phase J — Client: i18n

### Task J1: Neue Fluent-Keys

**Files:**
- Modify: `client/locales/de/main.ftl`
- Modify: `client/locales/en/main.ftl`
- Modify: `client/locales/fr/main.ftl`

- [ ] **Step 1: Anhaengen in `de/main.ftl`**

```
## Column-Editor (Q0005)
column-editor-title        = Spalte „{ $name }"
column-editor-visibility   = Sichtbar
column-editor-position     = Position
column-editor-min-width    = Min-Breite
column-editor-label        = Label
column-editor-sortable     = Sortierbar
column-editor-filter       = Filter
column-editor-format       = Format
column-editor-reset        = Zurücksetzen
column-editor-preview      = Vorschau

table-actions-edit-mode    = Layout bearbeiten
table-actions-save-view    = Speichern
table-actions-discard-view = Verwerfen
table-status-edit-layer    = Layer: { $layer }
table-status-pending       = { $n } ungespeicherte Änderungen
table-fallback-view        = Ansicht „{ $name }" nicht gefunden — zeige Default

## Filter-Labels
filter-contains = Enthält
filter-equals   = Gleich
filter-range    = Bereich

## Formatter-Labels
formatter-money-symbol    = EUR-Symbol €
formatter-money-code      = EUR-Code
formatter-money-decimals  = Nur Dezimalen
formatter-decimal-default = Standard
formatter-decimal-2       = 2 Nachkommastellen
formatter-date-iso        = ISO (YYYY-MM-DD)
formatter-date-local      = Lokal
formatter-datetime-iso    = ISO Datum+Zeit
formatter-datetime-local  = Lokal Datum+Zeit
formatter-int-default     = Standard
formatter-bool-yesno      = Ja / Nein
formatter-text-plain      = Klartext
```

- [ ] **Step 2: Englisch parallel anhaengen** (analog `en/main.ftl`)

```
## Column-Editor (Q0005)
column-editor-title        = Column "{ $name }"
column-editor-visibility   = Visible
column-editor-position     = Position
column-editor-min-width    = Min width
column-editor-label        = Label
column-editor-sortable     = Sortable
column-editor-filter       = Filter
column-editor-format       = Format
column-editor-reset        = Reset
column-editor-preview      = Preview

table-actions-edit-mode    = Edit layout
table-actions-save-view    = Save
table-actions-discard-view = Discard
table-status-edit-layer    = Layer: { $layer }
table-status-pending       = { $n } unsaved changes
table-fallback-view        = View "{ $name }" not found — showing default

## Filter-Labels
filter-contains = Contains
filter-equals   = Equals
filter-range    = Range

## Formatter-Labels
formatter-money-symbol    = EUR symbol €
formatter-money-code      = EUR code
formatter-money-decimals  = Decimals only
formatter-decimal-default = Default
formatter-decimal-2       = 2 decimals
formatter-date-iso        = ISO (YYYY-MM-DD)
formatter-date-local      = Local
formatter-datetime-iso    = ISO Date+Time
formatter-datetime-local  = Local Date+Time
formatter-int-default     = Default
formatter-bool-yesno      = Yes / No
formatter-text-plain      = Plain text
```

- [ ] **Step 3: Franzoesisch parallel anhaengen** (analog `fr/main.ftl`)

```
## Column-Editor (Q0005)
column-editor-title        = Colonne « { $name } »
column-editor-visibility   = Visible
column-editor-position     = Position
column-editor-min-width    = Largeur min
column-editor-label        = Libellé
column-editor-sortable     = Triable
column-editor-filter       = Filtre
column-editor-format       = Format
column-editor-reset        = Réinitialiser
column-editor-preview      = Aperçu

table-actions-edit-mode    = Modifier la mise en page
table-actions-save-view    = Enregistrer
table-actions-discard-view = Annuler
table-status-edit-layer    = Couche : { $layer }
table-status-pending       = { $n } modifications non enregistrées
table-fallback-view        = Vue « { $name } » introuvable — affichage par défaut

## Filter-Labels
filter-contains = Contient
filter-equals   = Égal
filter-range    = Plage

## Formatter-Labels
formatter-money-symbol    = Symbole € EUR
formatter-money-code      = Code EUR
formatter-money-decimals  = Décimales uniquement
formatter-decimal-default = Par défaut
formatter-decimal-2       = 2 décimales
formatter-date-iso        = ISO (AAAA-MM-JJ)
formatter-date-local      = Local
formatter-datetime-iso    = ISO Date+Heure
formatter-datetime-local  = Local Date+Heure
formatter-int-default     = Par défaut
formatter-bool-yesno      = Oui / Non
formatter-text-plain      = Texte brut
```

- [ ] **Step 4: Build-Check**

Run: `cargo check -p client --target wasm32-unknown-unknown`
Expected: kein Compile-Fehler.

- [ ] **Step 5: Commit**

```bash
git add client/locales/de/main.ftl client/locales/en/main.ftl client/locales/fr/main.ftl
git commit -m "feat(client): i18n keys for column-editor + edit-mode + formatters (Q0005 J1)"
```

---

## Phase K — Client: ColumnEditorPopover-Komponente

### Task K1: Skeleton + Felder

**Files:**
- Create: `client/src/components/table/column_editor/popover.rs`
- Modify: `client/src/components/table/column_editor/mod.rs` (`pub mod popover;` + Re-Export)

- [ ] **Step 1: Modul registrieren**

In `client/src/components/table/column_editor/mod.rs` (ganz oben):

```rust
pub mod popover;
pub use popover::ColumnEditorPopover;
```

- [ ] **Step 2: Komponente schreiben** in `client/src/components/table/column_editor/popover.rs`

```rust
//! Q0005 — In-Place Column-Editor als Popover neben dem Spalten-Header.

use leptos::prelude::*;
use shared::view::ViewPropertyOverride;
use shared::{ColumnMeta, FieldType, Visibility};

use crate::components::table::filters::default_registry;
use crate::components::table::formatters::compatible_formatters_for;
use crate::i18n::t;
use crate::styling::{use_design, ButtonVariant, SurfaceLevel, TextVariant};

#[component]
pub fn ColumnEditorPopover(
    column: ColumnMeta,
    current_override: Signal<Option<ViewPropertyOverride>>,
    #[prop(into)] on_change: Callback<ViewPropertyOverride>,
    #[prop(into)] on_reset:  Callback<()>,
    #[prop(into)] on_close:  Callback<()>,
    /// Absolute Position relativ zum Viewport (px).
    top:  f64,
    left: f64,
) -> impl IntoView {
    let design = use_design();
    let card = design.surface(SurfaceLevel::Floating).inline.clone();
    let h3 = design.text(TextVariant::H3).inline.clone();
    let muted = design.text(TextVariant::Muted).inline.clone();
    let input_style = design.input().inline.clone();
    let secondary = design.button(ButtonVariant::Secondary).inline.clone();

    let col_key = column.key.clone();
    let col_field_type = column.field_type.clone();

    // Aktuelle Werte aus Override oder ColumnMeta-Default
    let ov = move || current_override.get().unwrap_or(ViewPropertyOverride {
        key: col_key.clone(),
        ..Default::default()
    });

    let visible    = move || ov().visibility.unwrap_or(Visibility::Visible) == Visibility::Visible;
    let order_val  = move || ov().order.unwrap_or(0);
    let width_val  = move || ov().min_width.unwrap_or(0);
    let label_val  = move || ov().label_override_key.unwrap_or_default();
    let sortable   = move || ov().sortable.unwrap_or(column.sortable);
    let filter_id  = move || ov().filter_id_override.unwrap_or_default();
    let format_id  = move || ov().formatter_id_override.unwrap_or_default();

    let filters = StoredValue::new(default_registry().compatible_for(&col_field_type));
    let formatters = StoredValue::new(compatible_formatters_for(&col_field_type));

    let emit = move |patch: Box<dyn FnOnce(&mut ViewPropertyOverride)>| {
        let mut next = ov();
        patch(&mut next);
        on_change.run(next);
    };

    let style = format!("{card} position: absolute; top: {top}px; left: {left}px; width: 280px; z-index: 1000;");

    view! {
        <div style=style on:keydown=move |ev| {
            if ev.key() == "Escape" { on_close.run(()); }
        }>
            <div style="display: flex; justify-content: space-between; align-items: center;">
                <h3 style=h3>{move || crate::t!("column-editor-title", "name" => column.label_key.clone())}</h3>
                <button style=secondary.clone() on:click=move |_| on_reset.run(())>
                    {move || t("column-editor-reset")}
                </button>
                <button style=secondary.clone() on:click=move |_| on_close.run(())>"✕"</button>
            </div>

            <label style="display: flex; align-items: center; gap: 0.5rem; margin-top: 0.5rem;">
                <input
                    type="checkbox"
                    prop:checked=move || visible()
                    on:change=move |ev| {
                        let checked = event_target_checked(&ev);
                        emit(Box::new(move |p| p.visibility = Some(if checked { Visibility::Visible } else { Visibility::Hidden })));
                    }
                />
                {move || t("column-editor-visibility")}
            </label>

            <label style="display: flex; gap: 0.5rem; margin-top: 0.5rem;">
                <span>{move || t("column-editor-position")}</span>
                <input
                    type="number"
                    style=input_style.clone()
                    prop:value=move || order_val()
                    on:input=move |ev| {
                        if let Ok(o) = event_target_value(&ev).parse::<i32>() {
                            emit(Box::new(move |p| p.order = Some(o)));
                        }
                    }
                />
            </label>

            <label style="display: flex; gap: 0.5rem; margin-top: 0.5rem;">
                <span>{move || t("column-editor-min-width")}</span>
                <input
                    type="number"
                    style=input_style.clone()
                    prop:value=move || width_val()
                    on:input=move |ev| {
                        if let Ok(w) = event_target_value(&ev).parse::<u32>() {
                            emit(Box::new(move |p| p.min_width = Some(w)));
                        }
                    }
                />
                <span>"px"</span>
            </label>

            <label style="display: flex; gap: 0.5rem; margin-top: 0.5rem;">
                <span>{move || t("column-editor-label")}</span>
                <input
                    type="text"
                    style=input_style.clone()
                    prop:value=move || label_val()
                    on:input=move |ev| {
                        let s = event_target_value(&ev);
                        emit(Box::new(move |p| p.label_override_key = if s.is_empty() { None } else { Some(s) }));
                    }
                />
            </label>

            <label style="display: flex; align-items: center; gap: 0.5rem; margin-top: 0.5rem;">
                <input
                    type="checkbox"
                    prop:checked=move || sortable()
                    on:change=move |ev| {
                        let c = event_target_checked(&ev);
                        emit(Box::new(move |p| p.sortable = Some(c)));
                    }
                />
                {move || t("column-editor-sortable")}
            </label>

            <label style="display: flex; gap: 0.5rem; margin-top: 0.5rem;">
                <span>{move || t("column-editor-filter")}</span>
                <select
                    style=input_style.clone()
                    on:change=move |ev| {
                        let id = event_target_value(&ev);
                        emit(Box::new(move |p| p.filter_id_override = if id.is_empty() { None } else { Some(id) }));
                    }
                >
                    <option value="" selected=move || filter_id().is_empty()>"—"</option>
                    {move || filters.with_value(|fs| fs.iter().map(|f| {
                        let id = f.id.clone(); let label = f.label_key.clone();
                        let selected = filter_id() == id;
                        view! { <option value=id.clone() selected=selected>{label}</option> }
                    }).collect_view())}
                </select>
            </label>

            <label style="display: flex; gap: 0.5rem; margin-top: 0.5rem;">
                <span>{move || t("column-editor-format")}</span>
                <select
                    style=input_style.clone()
                    on:change=move |ev| {
                        let id = event_target_value(&ev);
                        emit(Box::new(move |p| p.formatter_id_override = if id.is_empty() { None } else { Some(id) }));
                    }
                >
                    <option value="" selected=move || format_id().is_empty()>"—"</option>
                    {move || formatters.with_value(|fs| fs.iter().map(|f| {
                        let id = f.id.clone(); let label = f.label_key.clone();
                        let selected = format_id() == id;
                        view! { <option value=id.clone() selected=selected>{label}</option> }
                    }).collect_view())}
                </select>
            </label>

            <div style=format!("{muted} margin-top: 0.75rem; padding: 6px 8px; border-top: 1px solid #374151;")>
                {move || t("column-editor-preview")} ": "
                <code>{move || format!("{:?}", col_field_type)}</code>
            </div>
        </div>
    }
}
```

> Wenn `Design::SurfaceLevel` keine `Floating`-Variante hat, im `styling/tokens.rs` analog zu `Card` einfuegen (z. B. mit etwas mehr Shadow). Wenn `Design::input()` nicht existiert, eine schlanke Methode in `styling/inline.rs` ergaenzen, die ein Border + Padding-Style produziert.

- [ ] **Step 3: Compile-Check**

Run: `cargo check -p client --target wasm32-unknown-unknown`
Expected: kein Fehler.

- [ ] **Step 4: Commit**

```bash
git add client/src/components/table/column_editor/
git commit -m "feat(client): ColumnEditorPopover component (Q0005 K1)"
```

---

## Phase L — Client: EntityListPage-Integration

### Task L1: View-State, View-Picker via URL-Param, Edit-Mode, Pending-Buffer

**Files:**
- Modify: `client/src/routes/mod.rs` (`EntityListPage`)

- [ ] **Step 1: Imports und State erweitern**

Am Anfang von `EntityListPage`, nach dem bestehenden `let auth = AuthContext::use_context();`:

```rust
use std::collections::HashMap;
use shared::view::{ViewLayer, ViewPropertyOverride};
use crate::components::table::{apply_pending_overrides, ColumnEditorPopover};

let view_name = move || {
    leptos_router::hooks::use_query_map()
        .with(|q| q.get("view").map(|s| s.to_string()))
        .unwrap_or_else(|| "default".into())
};
let edit_mode: RwSignal<bool> = RwSignal::new(false);
let edit_layer: RwSignal<ViewLayer> = RwSignal::new(ViewLayer::Global); // MVP konstant
let pending_overrides: RwSignal<HashMap<String, ViewPropertyOverride>> =
    RwSignal::new(HashMap::new());
let open_popover_for: RwSignal<Option<(String, f64, f64)>> = RwSignal::new(None); // (col_key, top, left)
let current_view_version: RwSignal<Option<i32>> = RwSignal::new(None);

let current_view: LocalResource<Option<shared::view::EntityView>> = LocalResource::new(move || {
    let et = params.read().get("entity_type").unwrap_or_default();
    let vn = view_name();
    async move {
        if et.is_empty() { None }
        else { crate::graphql::queries::fetch_entity_view(&et, &vn).await.ok().flatten() }
    }
});
```

- [ ] **Step 2: Im Render `current_view.version` in `current_view_version` spiegeln**

Innerhalb des Render-Blocks von `EntityListPage`, vor dem Aufbau des `EntityTableShell`, ergaenze:

```rust
if let Some(Some(v)) = current_view.get().map(|r| r.take()) {
    current_view_version.set(Some(v.version));
}
```

- [ ] **Step 3: Pending-Overrides auf `cols` + `settings` anwenden**

In `EntityListPage` (im inner-Closure, nach `apply_settings_to_columns`):

```rust
let mut settings_for_table = settings.clone().unwrap_or_default();
apply_pending_overrides(&mut columns, &mut settings_for_table, &pending_overrides.get());
```

- [ ] **Step 4: Commit**

```bash
git add client/src/routes/mod.rs
git commit -m "feat(client): EntityListPage view state + pending overrides scaffolding (Q0005 L1)"
```

### Task L2: TopMenu-Erweiterung (Edit-Mode-Toggle + Save/Discard)

**Files:**
- Modify: `client/src/routes/mod.rs`

- [ ] **Step 1: TopMenu-Block erweitern**

Suche im Render-Body den bestehenden `<TopMenu>...</TopMenu>`-Block. Erweitere ihn um:

```rust
<TopMenu>
    <GlobalFilter/>
    {can_create.then(|| view! {
        <a href=new_href style=primary_btn.clone()>
            {move || t("table.actions.new")}
        </a>
    })}
    {can_update.then(|| view! {
        <button
            style=secondary_btn.clone()
            on:click=move |_| edit_mode.update(|b| *b = !*b)
        >
            {move || if edit_mode.get() { t("table.actions.discard-view") } else { t("table.actions.edit-mode") }}
        </button>
    })}
    {move || (edit_mode.get() && !pending_overrides.with(|p| p.is_empty())).then(|| view! {
        <button style=primary_btn.clone() on:click=move |_| {
            // Save-Flow → Task L4
            on_save.run(());
        }>
            {move || t("table.actions.save-view")}
        </button>
    })}
    {move || edit_mode.get().then(|| view! {
        <span style=muted.clone()>
            {move || crate::t!("table.status.edit-layer", "layer" => format!("{:?}", edit_layer.get()))}
            " · "
            {move || crate::t!("table.status.pending", "n" => pending_overrides.with(|p| p.len()) as i64)}
        </span>
    })}
</TopMenu>
```

> Wenn `muted` noch nicht im Scope ist: am Anfang des Closures hinzufuegen
> (`let muted = use_design().text(TextVariant::Muted).inline.clone();`).
> Wenn `primary_btn` ueber `secondary_btn` schon definiert ist — uebernehmen.

- [ ] **Step 2: Compile + Smoke-Open**

Run: `cargo check -p client --target wasm32-unknown-unknown`
Expected: kein Fehler.

Run separat (falls Trunk laeuft): kurzer Browser-Reload — der Edit-Mode-Toggle erscheint sichtbar in der TopMenu jeder Entity-Liste.

- [ ] **Step 3: Commit**

```bash
git add client/src/routes/mod.rs
git commit -m "feat(client): TopMenu edit-mode toggle + status pill (Q0005 L2)"
```

### Task L3: Header-Klick öffnet Popover

**Files:**
- Modify: `client/src/components/table/view.rs` (Header-Rendering im `<th>`)
- Modify: `client/src/routes/mod.rs` (Popover rendern an Page-Level)

- [ ] **Step 1: Edit-Mode-Context provide**

In `EntityListPage` (vor `EntityTableShell`):

```rust
provide_context::<RwSignal<bool>>(edit_mode);
provide_context::<RwSignal<Option<(String, f64, f64)>>>(open_popover_for);
```

- [ ] **Step 2: Header-Klick-Handler** in `client/src/components/table/view.rs`

Suche das `<th ...>`-Element fuer Spalten-Header (vermutlich um Zeile 100). Ergaenze:

```rust
let edit_mode_ctx = use_context::<RwSignal<bool>>();
let open_popover  = use_context::<RwSignal<Option<(String, f64, f64)>>>();
// ...
<th
    style=…
    on:click=move |ev| {
        let Some(em) = edit_mode_ctx else { return; };
        if !em.get() { return; }
        let Some(pop) = open_popover else { return; };
        let target = ev.target().and_then(|t| t.dyn_into::<web_sys::Element>().ok());
        let (top, left) = target.and_then(|el| el.get_bounding_client_rect().ok()).map(|r| (r.bottom(), r.left())).unwrap_or((0.0, 0.0));
        pop.set(Some((col.key.clone(), top, left)));
    }
>
    …
</th>
```

> `dyn_into` braucht `wasm_bindgen::JsCast` im Scope. Falls noetig importieren.

- [ ] **Step 3: Popover-Rendering in `EntityListPage`** (am Ende des Render-Body, ausserhalb von EntityTableShell)

```rust
{move || open_popover_for.get().map(|(key, top, left)| {
    let Some(col) = columns.iter().find(|c| c.key == key).cloned() else { return view! {}.into_any(); };
    let ov_sig: Signal<Option<ViewPropertyOverride>> = Signal::derive(move || {
        pending_overrides.with(|p| p.get(&key).cloned())
    });
    let key_for_change = key.clone();
    let key_for_reset = key.clone();
    view! {
        <ColumnEditorPopover
            column=col
            current_override=ov_sig
            on_change=move |ov| {
                pending_overrides.update(|m| { m.insert(key_for_change.clone(), ov); });
            }
            on_reset=move |_| {
                pending_overrides.update(|m| { m.remove(&key_for_reset); });
            }
            on_close=move |_| open_popover_for.set(None)
            top=top
            left=left
        />
    }.into_any()
})}
```

- [ ] **Step 4: Compile + Smoke**

Run: `cargo check -p client --target wasm32-unknown-unknown`
Expected: kein Fehler.

Browser-Smoke: Edit-Mode an → auf Header klicken → Popover oeffnet sich an der Header-Position.

- [ ] **Step 5: Commit**

```bash
git add client/src/components/table/view.rs client/src/routes/mod.rs
git commit -m "feat(client): header click opens ColumnEditorPopover in edit mode (Q0005 L3)"
```

### Task L4: Save-Flow + Conflict-Behandlung

**Files:**
- Modify: `client/src/routes/mod.rs` (Save-Handler `on_save`)

- [ ] **Step 1: `on_save`-Closure einfuegen** (vor TopMenu-Block)

```rust
let on_save: Callback<()> = Callback::new(move |_| {
    let et = params.read().get("entity_type").unwrap_or_default();
    let vn = view_name();
    let expected = current_view_version.get();
    let overrides: Vec<ViewPropertyOverride> = pending_overrides.with(|p| p.values().cloned().collect());
    let payload = serde_json::json!({
        "properties": overrides,
        "defaultFilter": serde_json::Value::Null,
        "defaultSort":   serde_json::Value::Null,
        "defaultPageSize": serde_json::Value::Null,
    });
    leptos::task::spawn_local(async move {
        let res = crate::graphql::queries::save_entity_view(
            crate::graphql::queries::SaveEntityViewInputClient {
                entity_type: &et,
                view_name:   &vn,
                layer:       ViewLayer::Global,
                owner_id:    None,
                payload,
                expected_version: expected,
            }
        ).await;
        match res {
            Ok(outcome) if outcome.kind == "OK" => {
                pending_overrides.update(|p| p.clear());
                edit_mode.set(false);
                open_popover_for.set(None);
                current_view.refetch();
            }
            Ok(outcome) if outcome.kind == "CONFLICT" => {
                if let Some(server) = outcome.view {
                    current_view_version.set(Some(server.version));
                }
                web_sys::window()
                    .and_then(|w| w.alert_with_message(
                        "Konflikt: andere Bearbeitung wurde zwischenzeitlich gespeichert. Bitte neu laden und Edits nochmal anwenden."
                    ).ok());
            }
            Ok(outcome) => {
                log::warn!("save_entity_view: {}: {:?}", outcome.kind, outcome.message);
            }
            Err(e) => log::error!("save_entity_view RPC: {e}"),
        }
    });
});
```

> `current_view.refetch()` ist die Leptos-LocalResource-API; pruefe Signatur (eventuell `current_view.refetch()` ohne await — siehe bestehende Vorlagen im File).

- [ ] **Step 2: Compile + Smoke**

Run: `cargo check -p client --target wasm32-unknown-unknown`
Expected: kein Fehler.

Browser-Smoke:
- Edit-Mode an → Spalte verstecken im Popover → „Speichern" → Pill „0 ungespeicherte Änderungen" → Edit-Mode aus → Reload → Spalte bleibt versteckt.
- Zwei Tabs parallel: Tab A speichern → Tab B speichern → Tab B bekommt Alert „Konflikt".

- [ ] **Step 3: Commit**

```bash
git add client/src/routes/mod.rs
git commit -m "feat(client): save_entity_view flow + conflict alert (Q0005 L4)"
```

### Task L5: Sample-Data-Fallback bei 0 Rows + Edit-Mode

**Files:**
- Modify: `client/src/routes/mod.rs`

- [ ] **Step 1: `display_source` zwischenschalten**

Suche `let source = Rc::new(RemoteSource::new(entity_type.clone()))`. Ersetze die Verwendung in `EntityTableShell` durch einen Memo-Wert, der bei `edit_mode && rows.is_empty()` auf `BuilderPreviewSource` umschaltet:

```rust
use crate::components::table::{BuilderPreviewSource, DEFAULT_PREVIEW_ROWS, RemoteSource};

let remote = Rc::new(RemoteSource::new(entity_type.clone())) as Rc<dyn crate::components::table::DataSource>;

// Im EntityTableShell-Aufruf statt `source=source`:
let preview_columns = columns.clone();
let display_source = if edit_mode.get() /* && rows_empty */ {
    Rc::new(BuilderPreviewSource::from_columns(&preview_columns, DEFAULT_PREVIEW_ROWS))
        as Rc<dyn crate::components::table::DataSource>
} else {
    remote.clone()
};
// source=display_source
```

> Hinweis: das tatsaechliche „rows.is_empty"-Signal kommt aus dem `TableState`,
> nicht aus dem EntityListPage-Scope. Vereinfachter MVP-Cut: schalte den
> Fallback **nur** auf `edit_mode` (nicht zusaetzlich auf 0 Rows). Das ist
> akzeptabel, weil der Edit-Mode ein bewusster User-Akt ist und die echten
> Rows in der Designer-Erfahrung temporaer durch Demo-Rows ersetzt werden
> duerfen. Wenn ein finaler 0-Row-Switch noetig wird, ziehe das in ein
> Folge-Q-Item — der Spec sagt dazu „nur Edit-Mode + 0 Rows", was wir
> exakt-genauso bauen koennen, sobald `TableState` einen `rows_count`-Signal
> exportiert (heute nicht der Fall, sicht- und testbar im
> Vorhandenen-Stand).

- [ ] **Step 2: Compile**

Run: `cargo check -p client --target wasm32-unknown-unknown`
Expected: kein Fehler.

- [ ] **Step 3: Commit**

```bash
git add client/src/routes/mod.rs
git commit -m "feat(client): edit-mode swaps to BuilderPreviewSource for sample data (Q0005 L5)"
```

---

## Phase M — Manual Browser-Smoke (Akzeptanz)

### Task M1: Akzeptanz-Checkliste durchgehen

**Files:** None (manual)

- [ ] **Step 1: Voraussetzungen**

- Server laeuft mit D2V-Daten:
  `D2V_LEGACY_URL=sqlite:///C:/Users/jz/data/d2v-smoke-copy.db cargo run -p server -- --data-dir ./examples/d2v`
- Client laeuft: `cd client && trunk serve`
- Browser auf http://127.0.0.1:8080
- Logged in als `bookkeeper@local` / `bookkeeper`

- [ ] **Step 2: Default-View bearbeiten**

1. `/entities/datev_entry` oeffnen
2. „Layout bearbeiten" klicken → TopMenu zeigt Layer-Pill + 0-Pending
3. Header von „Betrag" anklicken → Popover oeffnet
4. „Sichtbar" abhaken → Spalte verschwindet, Pending: 1
5. „Position" auf 1 setzen → Reihenfolge aendert sich
6. „Min-Breite" auf 150 → Spalte wird breiter
7. „Filter" Dropdown → „Range" auswaehlen
8. „Format" Dropdown → „EUR-Symbol €" auswaehlen
9. „Speichern" klicken → Edit-Mode aus, Pending 0
10. Browser-Reload → Aenderungen sind persistent (DB ist `sqlite::memory:` —
    Aenderungen ueberleben nur den Browser-Reload, nicht den Server-Neustart.
    Fuer Cross-Session-Test:
    `DBLICIOUS_DATABASE_URL=sqlite://./dblicious-d2v.db` setzen.)

- [ ] **Step 3: Reset einer Spalte**

1. Edit-Mode an
2. Popover fuer „Betrag" oeffnen → „Reset" → Spalte zurueck auf Default
3. „Speichern" → Server hat Override fuer „Betrag" entfernt

- [ ] **Step 4: View-Name unbekannt**

1. URL `/entities/datev_entry?view=irgendwas`
2. Erwartung: Tabelle laedt mit Default; Fallback-Pill „Ansicht 'irgendwas' nicht gefunden — zeige Default" sichtbar
   (das Pill-Rendering kommt aus dem `X-Dblicious-Fallback-View`-Header — ggf.
   nachreichen, falls vergessen: im server::schema-Resolver `Response::http_response_mut().headers_mut().insert(...)` setzen).

- [ ] **Step 5: 0-Row-Fall (Edit-Mode-Sample-Data)**

1. Edit-Mode an
2. Beobachtung: Demo-Rows mit synthetischen Werten erscheinen (E8: nur, wenn `rows.is_empty()`; MVP-Cut: greift in jedem Edit-Mode — siehe L5-Notiz)

- [ ] **Step 6: Conflict**

1. Tab A: Edit-Mode an, Aenderung, NICHT speichern
2. Tab B: Edit-Mode an, andere Aenderung, Speichern
3. Tab A: Speichern → Alert „Konflikt"

- [ ] **Step 7: Pruefe Logs**

Server-Konsole sollte zeigen:
- INFO `seed_entity_views_from_example`-Hinweise beim Boot
- INFO `view '<entity>/<name>' enthielt N Override(s) fuer unbekannte Spalten` bei Schema-Drift-Szenarien (nur falls die Test-DB von Hand modifiziert wurde)

- [ ] **Step 8: Sign-Off**

Wenn alle Punkte gruen: Q0005-Akzeptanz erreicht. Trage die Beobachtung im Queue-Item-File `docs/queue/Q0005-…` unter „Smoke-Test verified at HEAD <sha>" ein. Setze `status: reviewed` (oder `done` nach Review-Subagent).

```bash
git add docs/queue/Q0005-in-place-column-editor-auf-entity-table.md
git commit -m "chore(queue): Q0005 smoke-test verified"
```

---

## Self-Review

**1. Spec coverage:**
- Sektion 3 (Wire-Types) → Task A1
- Sektion 4.1 (SeaORM-Entity) → Task B1+B2
- Sektion 4.2 (Resolver) → Task C1+D1
- Sektion 4.3 (GraphQL-API) → Task E1+E2
- Sektion 4.4 (Loader-Bootstrap) → Task F1
- Sektion 4.5 (Auth) → in Task E1 als Auth-Gate im Mutation-Resolver
- Sektion 5.1 (Client-GraphQL-Adapter) → Task H1
- Sektion 5.2 (EntityListPage-State) → Task L1
- Sektion 5.3 (View-Picker via URL-Param) → Task L1
- Sektion 5.4 (Sample-Data-Fallback) → Task L5
- Sektion 5.5 (apply_pending_overrides) → Task I1
- Sektion 5.6 (Save-Flow) → Task L4
- Sektion 6.1 (TopMenu-Erweiterung) → Task L2
- Sektion 6.2 (ColumnEditorPopover) → Task K1
- Sektion 6.3 (Drag-Reorder) → Task I2 (pure func); UI-Verdrahtung verbleibt im Popover-Header (Task L3+K1) — der Drag-Handler am Header in L3 nutzt `compute_reorder`. **Lücke:** explizite Drag-Verdrahtung im `<th>` mit pointerdown/move/up nicht detailliert ausgeschrieben → erste Iteration ohne Drag, Reorder via Popover-Order-Input. Drag-Verdrahtung als Folge-Task ausgewiesen, sobald MVP-UI getestet ist.
- Sektion 6.4 (Filter/Formatter-Discovery) → Task I3+I4
- Sektion 6.5 (i18n) → Task J1
- Sektion 6.6 (A11y) → Esc in K1; voll abgedeckte Tab-Reihenfolge folgt iterativ
- Sektion 8 (Edge Cases E1-E9) → Tests in C1, D1, E1, G1; E8 als MVP-Cut in L5 dokumentiert

**Gefundene Lücke**: Drag-Reorder am Header (Sektion 6.3) ist als pure func (I2) plus UI-Hooks (L3) abgedeckt, aber nicht zu einer expliziten Drag-Animation/`pointermove`-Schleife im `<th>` ausgeschrieben. Das ist bewusst — die MVP-UI nutzt zunaechst den Order-Input im Popover; Drag-Reorder ist eine Folge-Verfeinerung (Task L6 als optional, post-MVP). Wenn der User Drag im MVP will, hier ein Task L6 anhaengen.

**2. Placeholder scan:** kein TBD/TODO im Plan. Code-Bloecke vollstaendig.

**3. Type consistency:** `ViewLayer`, `ViewPropertyOverride`, `EntityView`, `ResolvedView`, `ResolvedLayerRef`, `EntityViewSummary`, `SaveEntityViewInputClient`, `SaveEntityViewOutcomeClient` durchgaengig konsistent benannt. Method-Namen: `merge_layers`, `resolve_view`, `strip_unknown_keys`, `upsert_entity_view`, `find_entity_view`, `find_entity_views`, `delete_entity_view`, `seed_entity_views_from_example`, `apply_pending_overrides`, `compute_reorder` — keine Drifts.

**4. Optional follow-up tasks:**
- **L6 (post-MVP)**: Header-Drag-Reorder mit `pointermove` + `compute_reorder`-Aufruf, schreibt `order`-Werte ins `pending_overrides`-Set.
- **L7 (post-MVP)**: Fallback-Pill rendert `X-Dblicious-Fallback-View`-Header.
- **L8 (post-MVP)**: Outside-Click-Handler fuer Popover (heute nur Esc/Close-Button).

---

## Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-05-21-q0005-named-views.md`.** Zwei Ausführungs-Optionen:

**1. Subagent-Driven (empfohlen)** — ich dispatche pro Task einen frischen Subagent, reviewe zwischen Tasks, schnelle Iteration. Passt zu der Groesse (13 Phasen, ~25 Tasks).

**2. Inline Execution** — wir gehen die Tasks in dieser Session durch mit `executing-plans`, Checkpoint-basiertes Batch-Ausführen. Mehr Kontext fuer mich, aber Risiko, Context-Budget knapp zu kriegen.

Welcher Weg?
