// Provider-Beispiel: formatiert einen Money-Wert als EUR-String.
// (Source ist hier nur Demo — Phase 3 inspiziert die Funktionssignatur
// nicht; das uebernimmt erst die Save-Pipeline.)
fn fmt(value, currency) {
    `${value} ${currency}`
}
